---
import AppPagination from '../../components/AppPagination.astro';
import FlashMessage from '../../components/FlashMessage.astro';
import SideNav from '../../components/SideNav.astro';
import TopBar from '../../components/TopBar.astro';
import Layout from '../../layouts/Layout.astro';
import '../../styles/global.css';

const token = Astro.locals.token ?? '';
const initialLoadError = token ? '' : 'No hay sesion valida para consultar cobros.';
---

<Layout>
	<cobros-manager>
		<div class="min-h-screen bg-(--page-background) transition-colors duration-300">
			<div
				class="app-shell grid min-h-screen overflow-hidden bg-transparent"
				data-app-shell
				data-sidebar-state="expanded"
			>
				<SideNav currentPath={Astro.url.pathname} />

				<div class="grid min-h-full grid-rows-[auto_1fr]">
					<TopBar />

					<main class="px-4 py-4 sm:px-6 sm:py-6">
						<section class="grid gap-4 sm:gap-5" data-requires-feature="DEPOSIT_COLLECTION">
							{
								initialLoadError ? (
									<FlashMessage message={initialLoadError} type="error" expand={true} dismissible={false} />
								) : (
									<div class="grid gap-3 sm:gap-4">
										<header class="grid gap-1">
											<h1 class="m-0 text-[1.5rem] font-bold leading-none tracking-tight text-(--on-surface) sm:text-[1.75rem]">
												Cobros{' '}
												<span class="font-semibold text-(--on-surface-variant)" data-cobros-summary>(0)</span>
											</h1>
											<p class="m-0 hidden text-[0.88rem] leading-snug text-(--on-surface-variant) sm:block">
												Revisá señas SIPAP y validá comprobantes pendientes.
											</p>
										</header>

										<select
											data-cobros-date-preset
											hidden
											class="hidden"
											aria-hidden="true"
											tabindex="-1"
										>
											<option value="this_month">Este mes</option>
											<option value="last_month">Mes pasado</option>
											<option value="custom">Personalizado</option>
										</select>

										<div class="flex items-start gap-2">
											<div class="hidden min-w-0 flex-1 flex-wrap gap-2 md:flex" role="tablist" aria-label="Estado">
												<button type="button" data-cobros-tab="all" class="cobros-tab is-active">Todos</button>
												<button type="button" data-cobros-tab="pending" class="cobros-tab">Pendientes</button>
												<button type="button" data-cobros-tab="approved" class="cobros-tab">Aprobados</button>
												<button type="button" data-cobros-tab="refunded" class="cobros-tab">Reembolsados</button>
												<button type="button" data-cobros-tab="expired" class="cobros-tab">Vencidos</button>
											</div>
											<button
												type="button"
												class="cobros-period-filter-btn relative ml-auto flex size-11 shrink-0 items-center justify-center rounded-2xl border border-(--shell-border) bg-(--surface-bright) text-(--on-surface-variant) shadow-sm transition hover:border-(--on-surface-variant) hover:text-(--on-surface) focus:border-(--primary) focus:outline-none focus:ring-4 focus:ring-(--primary)/10"
												data-open-period-filter
												aria-label="Filtrar por periodo"
												aria-haspopup="dialog"
												aria-controls="cobros-period-filter-sheet"
												aria-pressed="false"
											>
												<span class="material-symbols-rounded text-[1.25rem]" aria-hidden="true">tune</span>
												<span class="cobros-period-filter-badge hidden" data-period-filter-badge aria-hidden="true"></span>
											</button>
										</div>

										<div class="hidden rounded-2xl border border-rose-500/20 bg-rose-500/10 px-4 py-3 text-[0.9rem] font-bold text-rose-500" data-cobros-error></div>
										<div
											class="cobros-loading hidden"
											data-cobros-loading
											aria-busy="true"
											aria-label="Cargando cobros"
										>
											<div class="cobros-loading__grid" aria-hidden="true">
												{Array.from({ length: 3 }).map(() => (
													<article class="cobros-loading__card">
														<div class="cobros-loading__row">
															<div class="panel-skeleton-line h-4 w-28 rounded"></div>
															<div class="panel-skeleton-line h-6 w-16 rounded-full"></div>
														</div>
														<div class="panel-skeleton-line mt-3 h-5 w-40 max-w-full rounded-md"></div>
														<div class="panel-skeleton-line mt-2 h-3.5 w-48 max-w-[85%] rounded"></div>
														<div class="panel-skeleton-line mt-4 h-11 w-full rounded-full md:hidden"></div>
													</article>
												))}
											</div>
										</div>

										<div class="cobros-results overflow-hidden rounded-2xl border border-(--shell-border) bg-(--surface-bright)" data-cobros-results>
											<!-- Desktop headers -->
											<div class="hidden overflow-x-auto md:block" data-cobros-table-wrap>
												<table class="w-full min-w-[44rem] border-collapse text-left text-[0.92rem]">
													<thead>
														<tr class="border-b border-(--shell-border) text-[0.78rem] uppercase tracking-wide text-(--on-surface-variant)">
															<th class="px-4 py-3 font-bold">Fecha / Hora</th>
															<th class="px-4 py-3 font-bold">Cliente</th>
															<th class="px-4 py-3 font-bold">Servicio</th>
															<th class="px-4 py-3 font-bold">Monto</th>
															<th class="px-4 py-3 font-bold">Estado</th>
															<th class="px-4 py-3 font-bold"></th>
														</tr>
													</thead>
													<tbody data-cobros-table-body></tbody>
												</table>
											</div>

											<div class="hidden cobros-empty" data-cobros-empty role="status">
												<div class="cobros-empty__icon" aria-hidden="true">
													<span class="material-symbols-rounded">payments</span>
												</div>
												<h2 class="cobros-empty__title">No hay cobros</h2>
												<p class="cobros-empty__copy">
													No hay movimientos para el filtro seleccionado.
												</p>
											</div>

											<!-- Mobile cards -->
											<div class="cobros-cards grid gap-3 md:hidden" data-cobros-cards></div>
										</div>

										<AppPagination
											currentPage={1}
											totalPages={1}
											totalRecords={0}
											recordLabel="cobros"
											ariaLabel="Paginacion cobros"
											hidden={true}
											rootDataAttr="data-cobros-pagination"
											summaryDataAttr="data-cobros-page-label"
											pagesDataAttr="data-cobros-pagination-pages"
											prevDataAttr="data-cobros-prev"
											nextDataAttr="data-cobros-next"
											pageDataAttr="data-cobros-page"
										/>
									</div>
								)
							}
						</section>

						<section
							class="hidden rounded-2xl border border-(--shell-border) bg-(--surface-bright) p-6"
							data-cobros-feature-locked
						>
							<h2 class="text-xl font-bold text-(--on-surface)">Cobros es Premium</h2>
							<p class="mt-2 text-(--on-surface-variant)">
								El cobro de señas SIPAP está disponible en el plan Premium.
							</p>
							<a href="/panel/plan" class="mt-4 inline-flex h-11 items-center rounded-full bg-(--primary) px-5 text-sm font-semibold text-(--on-primary) no-underline">
								Ver planes
							</a>
						</section>
					</main>
				</div>
			</div>

			<dialog
				id="cobros-period-filter-sheet"
				class="cobros-period-filter-sheet"
				data-cobros-period-sheet
				aria-labelledby="cobros-period-filter-title"
			>
				<div class="cobros-period-filter-sheet__handle" aria-hidden="true">
					<span></span>
				</div>
				<div class="cobros-period-filter-sheet__header">
					<h2 id="cobros-period-filter-title" class="m-0 text-[1.05rem] font-extrabold tracking-tight">
						<span class="md:hidden">Filtros</span>
						<span class="hidden md:inline">Periodo</span>
					</h2>
					<button
						type="button"
						class="flex size-9 shrink-0 items-center justify-center rounded-full text-(--on-surface-variant) transition-colors hover:bg-(--surface-variant) hover:text-(--on-surface)"
						data-close-period-filter
						aria-label="Cerrar filtro"
					>
						<span class="material-symbols-rounded text-[1.3rem]">close</span>
					</button>
				</div>
				<div class="cobros-period-filter-sheet__body">
					<div class="cobros-filter-group md:hidden">
						<label class="cobros-filter-group__label" for="cobros-status-select">Estado</label>
						<div class="cobros-filter-select">
							<select id="cobros-status-select" data-cobros-status-select aria-label="Estado">
								<option value="all">Todos</option>
								<option value="pending">Pendientes</option>
								<option value="approved">Aprobados</option>
								<option value="refunded">Reembolsados</option>
								<option value="expired">Vencidos</option>
							</select>
							<span class="material-symbols-rounded cobros-filter-select__chevron" aria-hidden="true">expand_more</span>
						</div>
					</div>
					<p class="cobros-filter-group__label md:hidden">Periodo</p>
					<div class="cobros-period-filter-sheet__list" role="listbox" aria-label="Periodo">
						<button type="button" class="cobros-period-option" data-period-option="this_month" role="option">
							<span>Este mes</span>
							<span class="material-symbols-rounded" aria-hidden="true">check</span>
						</button>
						<button type="button" class="cobros-period-option" data-period-option="last_month" role="option">
							<span>Mes pasado</span>
							<span class="material-symbols-rounded" aria-hidden="true">check</span>
						</button>
						<button type="button" class="cobros-period-option" data-period-option="custom" role="option">
							<span>Personalizado</span>
							<span class="material-symbols-rounded" aria-hidden="true">check</span>
						</button>
					</div>
					<div class="cobros-period-custom hidden" data-cobros-custom-dates>
						<label class="grid gap-1.5 text-[0.85rem] font-bold text-(--on-surface)">
							Desde
							<span class="cobros-date-field">
								<input
									type="text"
									class="cobros-date-field__text"
									data-cobros-date-from-text
									inputmode="numeric"
									autocomplete="off"
									placeholder="dd/mm/aaaa"
									maxlength="10"
									aria-label="Fecha desde"
								/>
								<input type="hidden" data-cobros-date-from value="" />
								<button
									type="button"
									class="cobros-date-field__pick"
									data-cobros-date-from-pick
									aria-label="Elegir fecha desde"
								>
									<span class="material-symbols-rounded" aria-hidden="true">calendar_month</span>
								</button>
							</span>
						</label>
						<label class="grid gap-1.5 text-[0.85rem] font-bold text-(--on-surface)">
							Hasta
							<span class="cobros-date-field">
								<input
									type="text"
									class="cobros-date-field__text"
									data-cobros-date-to-text
									inputmode="numeric"
									autocomplete="off"
									placeholder="dd/mm/aaaa"
									maxlength="10"
									aria-label="Fecha hasta"
								/>
								<input type="hidden" data-cobros-date-to value="" />
								<button
									type="button"
									class="cobros-date-field__pick"
									data-cobros-date-to-pick
									aria-label="Elegir fecha hasta"
								>
									<span class="material-symbols-rounded" aria-hidden="true">calendar_month</span>
								</button>
							</span>
						</label>
						<p class="hidden m-0 text-[0.82rem] font-semibold text-rose-500" data-cobros-date-error></p>
						<button
							type="button"
							class="cobros-period-apply"
							data-apply-period-filter
						>
							Aplicar
						</button>
					</div>

					<dialog
						class="cobros-date-picker"
						data-cobros-date-picker
						aria-labelledby="cobros-date-picker-label"
					>
						<div class="dtp-card">
							<div class="dtp-head">
								<div class="dtp-head-row">
									<p class="dtp-target-label" id="cobros-date-picker-label" data-cobros-dp-label>
										Seleccionando fecha
									</p>
									<button
										type="button"
										class="dtp-icon-btn"
										data-cobros-dp-close
										aria-label="Cerrar selector de fecha"
									>
										<span class="material-symbols-rounded text-[1.1rem]">close</span>
									</button>
								</div>

								<div class="dtp-nav-row">
									<div class="inline-flex items-center gap-1.5">
										<select
											data-cobros-dp-month
											class="dtp-select"
											aria-label="Seleccionar mes"
										></select>
										<select
											data-cobros-dp-year
											class="dtp-select dtp-select--year"
											aria-label="Seleccionar año"
										></select>
									</div>
									<div class="inline-flex items-center gap-1">
										<button
											type="button"
											class="dtp-icon-btn"
											data-cobros-dp-prev
											aria-label="Mes anterior"
										>
											<span class="material-symbols-rounded text-[1.05rem]">chevron_left</span>
										</button>
										<button
											type="button"
											class="dtp-icon-btn"
											data-cobros-dp-next
											aria-label="Mes siguiente"
										>
											<span class="material-symbols-rounded text-[1.05rem]">chevron_right</span>
										</button>
									</div>
								</div>

								<div class="dtp-weekdays">
									<span>Lun</span>
									<span>Mar</span>
									<span>Mié</span>
									<span>Jue</span>
									<span>Vie</span>
									<span>Sáb</span>
									<span>Dom</span>
								</div>
								<div class="dtp-days-grid" data-cobros-dp-days></div>
							</div>

							<div class="dtp-foot">
								<div class="dtp-foot-row cobros-dp-foot-row">
									<button type="button" class="dtp-action-btn" data-cobros-dp-today>Hoy</button>
									<button type="button" class="dtp-action-btn dtp-action-btn--primary" data-cobros-dp-apply>
										Hecho
									</button>
								</div>
							</div>
						</div>
					</dialog>
				</div>
			</dialog>

			<dialog
				class="cobros-modal m-0 max-h-none w-screen max-w-none border-0 bg-transparent p-0 text-(--on-surface) shadow-none open:flex open:flex-col open:items-stretch open:justify-end sm:open:items-center sm:open:justify-center lg:open:flex-row lg:open:items-stretch lg:open:justify-end"
				data-cobros-modal
				aria-labelledby="cobros-modal-title"
			>
				<div
					class="cobros-modal__panel relative flex max-h-[92dvh] w-full max-w-none flex-col overflow-hidden rounded-t-[1.5rem] rounded-b-none border border-(--shell-border) bg-(--surface-bright) shadow-[0_24px_80px_rgba(0,0,0,0.2)] sm:max-h-[min(90vh,40rem)] sm:w-[min(28rem,calc(100vw-2rem))] sm:max-w-[28rem] sm:rounded-[1.5rem] lg:h-[100dvh] lg:max-h-[100dvh] lg:w-[min(26rem,100vw)] lg:max-w-[26rem] lg:rounded-none lg:border-y-0 lg:border-r-0 lg:shadow-none"
					data-cobros-modal-panel
				>
					<header class="flex shrink-0 items-start justify-between gap-3 border-b border-(--shell-border) px-5 py-4">
						<div>
							<h2 id="cobros-modal-title" class="m-0 text-lg font-bold" data-cobros-modal-title>Validar comprobante</h2>
							<p class="mt-1 text-sm text-(--on-surface-variant)" data-cobros-modal-subtitle></p>
						</div>
						<button type="button" class="grid size-10 place-items-center rounded-full hover:bg-(--control-surface)" data-cobros-modal-close aria-label="Cerrar">
							<span class="material-symbols-rounded">close</span>
						</button>
					</header>
					<div class="grid min-h-0 flex-1 content-start gap-5 overflow-y-auto overscroll-contain p-5">
						<div
							class="cobros-receipt-row hidden items-center gap-3 rounded-xl border border-(--shell-border) bg-(--control-surface) px-3.5 py-3"
							data-cobros-receipt-row
						>
							<span
								class="material-symbols-rounded shrink-0 text-[1.4rem] text-(--primary)"
								aria-hidden="true"
								data-cobros-receipt-icon
							>description</span>
							<div class="min-w-0 flex-1">
								<p class="m-0 truncate text-sm font-bold text-(--on-surface)" data-cobros-receipt-name>comprobante</p>
								<p class="m-0 text-xs text-(--on-surface-variant)">Comprobante adjunto</p>
							</div>
							<button
								type="button"
								class="cobros-receipt-open cursor-pointer"
								title="Ver comprobante"
								aria-label="Ver comprobante"
								data-cobros-receipt-open
							>
								<span class="material-symbols-rounded" aria-hidden="true">visibility</span>
							</button>
						</div>
						<p class="hidden rounded-xl border border-dashed border-(--shell-border) px-4 py-4 text-sm text-(--on-surface-variant)" data-cobros-modal-no-image>
							Sin comprobante adjunto.
						</p>

						<div class="cobros-ticket grid grid-cols-1 gap-4 sm:grid-cols-2 sm:gap-x-5 sm:gap-y-4" data-cobros-ticket>
							<div class="grid gap-1 min-w-0">
								<span class="text-[0.72rem] font-semibold uppercase tracking-wide text-(--on-surface-variant)">Cliente</span>
								<strong class="truncate text-[1.05rem] font-extrabold leading-snug" data-cobros-modal-customer>—</strong>
							</div>
							<div class="grid gap-1 min-w-0 sm:text-right">
								<span class="text-[0.72rem] font-semibold uppercase tracking-wide text-(--on-surface-variant)">Monto</span>
								<strong class="text-[1.15rem] font-extrabold leading-snug tabular-nums" data-cobros-modal-amount>—</strong>
							</div>
							<div class="grid gap-1 min-w-0">
								<span class="text-[0.72rem] font-semibold uppercase tracking-wide text-(--on-surface-variant)">Servicio</span>
								<strong class="truncate text-[0.98rem] font-bold leading-snug" data-cobros-modal-service>—</strong>
							</div>
							<div class="grid gap-1 min-w-0 sm:text-right">
								<span class="text-[0.72rem] font-semibold uppercase tracking-wide text-(--on-surface-variant)">Código HASEL</span>
								<strong class="truncate font-mono text-[0.92rem] font-bold leading-snug" data-cobros-modal-reference>—</strong>
							</div>
							<div class="grid gap-1 min-w-0">
								<span class="text-[0.72rem] font-semibold uppercase tracking-wide text-(--on-surface-variant)">Profesional</span>
								<strong class="truncate text-[0.98rem] font-bold leading-snug" data-cobros-modal-professional>—</strong>
							</div>
							<div class="grid gap-1 min-w-0 sm:text-right">
								<span class="text-[0.72rem] font-semibold uppercase tracking-wide text-(--on-surface-variant)">Lugar</span>
								<strong class="truncate text-[0.98rem] font-bold leading-snug" data-cobros-modal-location>—</strong>
							</div>
						</div>

						<div class="hidden grid gap-3" data-cobros-refund-block>
							<div class="grid gap-1 text-sm">
								<span class="text-(--on-surface-variant)">Estado reembolso</span>
								<strong data-cobros-modal-refund-status>—</strong>
							</div>
							<div class="grid gap-1 text-sm">
								<span class="text-(--on-surface-variant)">Alias SIPAP del cliente</span>
								<strong class="font-mono break-all" data-cobros-modal-refund-alias>—</strong>
							</div>
							<button
								type="button"
								class="hidden inline-flex h-12 items-center justify-center rounded-full bg-(--primary) px-5 text-base font-semibold text-(--on-primary)"
								data-cobros-mark-refund-sent
							>
								Marcar reembolso enviado
							</button>
							<p class="hidden text-sm font-semibold text-amber-700" data-cobros-claim-note></p>
							<div class="hidden grid gap-2" data-cobros-waive-wrap>
								<label class="grid gap-1 text-sm font-semibold">
									Renunciar al reembolso (motivo)
									<input type="text" maxlength="400" placeholder="Ej. cliente aceptó crédito / error de monto" class="cobros-select" data-cobros-waive-reason />
								</label>
								<button type="button" class="inline-flex h-11 items-center justify-center rounded-full border border-(--shell-border) px-5 text-sm font-semibold" data-cobros-waive>
									Marcar como renunciado (WAIVED)
								</button>
							</div>
						</div>
					</div>

					<footer class="cobros-modal-footer z-20 mt-auto shrink-0 border-t border-(--shell-border) bg-(--surface-bright) px-5 py-4">
						<div class="hidden" data-cobros-modal-actions>
							<div class="cobros-reject-reveal" data-cobros-reject-wrap>
								<div class="cobros-reject-alert" role="status">
									<span class="material-symbols-rounded cobros-reject-alert__icon" aria-hidden="true">warning</span>
									<p class="cobros-reject-alert__text">
										¿Confirmás el rechazo? El cliente deberá subir un nuevo comprobante.
									</p>
								</div>
								<label class="grid gap-1.5 pb-3.5 text-sm font-semibold">
									Motivo de rechazo (opcional)
									<input type="text" maxlength="400" placeholder="Ej. foto borrosa, monto distinto" class="cobros-select" data-cobros-reject-reason />
								</label>
							</div>
							<div class="cobros-modal-actions" data-cobros-actions-default>
								<button
									type="button"
									class="cobros-modal-btn cobros-modal-btn--ghost"
									data-cobros-reject
								>
									Rechazar
								</button>
								<button
									type="button"
									class="cobros-modal-btn cobros-modal-btn--primary"
									data-cobros-approve
								>
									Aprobar seña
								</button>
							</div>
							<div class="cobros-modal-actions hidden" data-cobros-actions-reject>
								<button
									type="button"
									class="cobros-modal-btn cobros-modal-btn--muted"
									data-cobros-reject-cancel
								>
									Cancelar
								</button>
								<button
									type="button"
									class="cobros-modal-btn cobros-modal-btn--danger"
									data-cobros-reject-confirm
								>
									Rechazar
								</button>
							</div>
						</div>
						<p class="text-sm text-(--on-surface-variant)" data-cobros-modal-status></p>
					</footer>
				</div>
			</dialog>

			<dialog class="cobros-viewer" data-cobros-viewer aria-label="Comprobante">
				<div class="cobros-viewer__bar">
					<p class="cobros-viewer__name" data-cobros-viewer-name>Comprobante</p>
					<a
						class="cobros-viewer__open"
						href="#"
						target="_blank"
						rel="noopener noreferrer"
						data-cobros-viewer-open
					>
						<span class="material-symbols-rounded text-[1.15rem]" aria-hidden="true">open_in_new</span>
						Abrir
					</a>
					<button type="button" class="cobros-viewer__close" data-cobros-viewer-close aria-label="Cerrar">
						<span class="material-symbols-rounded">close</span>
					</button>
				</div>
				<div class="cobros-viewer__body">
					<img alt="Comprobante" class="cobros-viewer__img hidden" data-cobros-viewer-img />
					<iframe class="cobros-viewer__frame hidden" title="Comprobante PDF" data-cobros-viewer-frame></iframe>
				</div>
			</dialog>
		</div>
	</cobros-manager>
</Layout>

<style>
	.cobros-tab {
		border-radius: 999px;
		border: 1px solid var(--shell-border);
		background: var(--surface-bright);
		padding: 0.55rem 1rem;
		font-size: 0.88rem;
		font-weight: 700;
		color: var(--on-surface);
		cursor: pointer;
	}
	.cobros-tab.is-active {
		background: var(--primary-container);
		color: var(--primary);
		border-color: transparent;
	}
	.cobros-select {
		border-radius: 1rem;
		border: 1px solid var(--shell-border);
		background: var(--surface-bright);
		padding: 0.65rem 0.9rem;
		font-size: 0.92rem;
		font-weight: 600;
		color: var(--on-surface);
		outline: none;
	}
	[data-cobros-table-wrap].is-empty,
	[data-cobros-cards].hidden,
	[data-cobros-results].hidden,
	.cobros-loading.hidden {
		display: none !important;
	}

	/* .app-pagination define display:flex y pisa la utilidad .hidden de Tailwind. */
	.app-pagination.hidden,
	.app-pagination[hidden] {
		display: none !important;
	}

	/* Móvil: sin caja externa → cards sueltas (como Clientes/Servicios) */
	@media (max-width: 767px) {
		.cobros-results {
			border: 0;
			background: transparent;
			border-radius: 0;
			overflow: visible;
			box-shadow: none;
		}
	}

	.cobros-loading {
		display: block;
	}

	.cobros-loading__grid {
		display: grid;
		gap: 0.75rem;
	}

	.cobros-loading__card {
		border-radius: 1.15rem;
		border: 1px solid var(--shell-border);
		background: var(--surface-bright);
		padding: 1rem 1.05rem;
	}

	.cobros-loading__row {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
	}

	@media (min-width: 768px) {
		.cobros-loading__grid {
			gap: 0.65rem;
		}

		.cobros-loading__card {
			border-radius: 1rem;
			padding: 0.95rem 1.15rem;
		}
	}

	.cobros-empty {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 0.35rem;
		padding: 1.75rem 1.25rem;
		text-align: center;
		background: transparent;
	}
	.cobros-empty.hidden {
		display: none;
	}
	.cobros-empty__icon {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 3.5rem;
		height: 3.5rem;
		margin-bottom: 0.5rem;
		border-radius: 999px;
		background: color-mix(in srgb, var(--surface-container) 88%, var(--surface-bright) 12%);
		color: var(--on-surface-variant);
	}
	.cobros-empty__icon .material-symbols-rounded {
		font-size: 1.75rem;
	}
	.cobros-empty__title {
		margin: 0;
		font-size: 1.1rem;
		font-weight: 800;
		color: var(--on-surface);
	}
	.cobros-empty__copy {
		margin: 0;
		max-width: 18rem;
		font-size: 0.95rem;
		font-weight: 600;
		line-height: 1.4;
		color: var(--on-surface-variant);
	}

	.cobros-period-filter-btn.is-active {
		border-color: color-mix(in srgb, var(--primary) 55%, var(--shell-border));
		color: var(--primary);
		background: color-mix(in srgb, var(--primary) 10%, var(--surface-bright));
	}
	.cobros-period-filter-badge {
		position: absolute;
		top: 0.45rem;
		right: 0.45rem;
		width: 0.45rem;
		height: 0.45rem;
		border-radius: 999px;
		background: var(--primary);
		box-shadow: 0 0 0 2px var(--surface-bright);
	}
	.cobros-period-filter-sheet {
		position: fixed;
		inset: auto 0 0;
		z-index: 80;
		margin: 0;
		width: 100vw;
		max-width: none;
		max-height: min(72dvh, 28rem);
		border: 1px solid var(--shell-border);
		border-bottom: 0;
		border-radius: 1.6rem 1.6rem 0 0;
		background: var(--surface-bright);
		color: var(--on-surface);
		padding: 0;
		overflow: hidden;
		box-shadow: 0 -12px 40px rgb(0 0 0 / 0.18);
	}
	.cobros-period-filter-sheet[open] {
		display: flex;
		flex-direction: column;
	}
	.cobros-period-filter-sheet::backdrop {
		background: rgb(0 0 0 / 0.4);
	}
	.cobros-period-filter-sheet__handle {
		display: flex;
		justify-content: center;
		padding: 0.65rem 0 0.15rem;
	}
	.cobros-period-filter-sheet__handle span {
		display: block;
		width: 2.4rem;
		height: 0.28rem;
		border-radius: 999px;
		background: color-mix(in srgb, var(--on-surface-variant) 45%, transparent);
	}
	.cobros-period-filter-sheet__header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
		padding: 0.35rem 1rem 0.75rem;
		border-bottom: 1px solid var(--shell-border);
	}
	.cobros-period-filter-sheet__body {
		display: grid;
		gap: 0.75rem;
		padding: 0.75rem 0.85rem max(0.95rem, env(safe-area-inset-bottom));
		overflow-y: auto;
		overscroll-behavior: contain;
		min-height: 0;
	}
	.cobros-period-filter-sheet__list {
		display: grid;
		gap: 0.35rem;
	}
	.cobros-filter-group {
		display: grid;
		gap: 0.4rem;
	}
	.cobros-filter-group__label {
		display: block;
		margin: 0;
		padding: 0 0.25rem;
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.06em;
		text-transform: uppercase;
		color: var(--on-surface-variant);
	}
	.cobros-filter-select {
		position: relative;
		display: flex;
		align-items: center;
	}
	.cobros-filter-select select {
		-webkit-appearance: none;
		appearance: none;
		width: 100%;
		height: 3rem;
		padding: 0 2.75rem 0 1rem;
		border-radius: 1rem;
		border: 1px solid var(--shell-border);
		background: var(--surface-bright);
		color: var(--on-surface);
		font-size: 0.95rem;
		font-weight: 700;
		cursor: pointer;
		transition: border-color 0.15s ease, box-shadow 0.15s ease;
	}
	.cobros-filter-select select:focus {
		outline: none;
		border-color: var(--primary);
		box-shadow: 0 0 0 4px color-mix(in srgb, var(--primary) 12%, transparent);
	}
	.cobros-filter-select__chevron {
		position: absolute;
		right: 0.85rem;
		font-size: 1.4rem;
		color: var(--on-surface-variant);
		pointer-events: none;
	}
	.cobros-period-option {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
		width: 100%;
		border: 1px solid transparent;
		border-radius: 1rem;
		background: transparent;
		padding: 0.85rem 0.95rem;
		text-align: left;
		font-size: 0.95rem;
		font-weight: 700;
		color: var(--on-surface);
		cursor: pointer;
	}
	.cobros-period-option:hover {
		background: color-mix(in srgb, var(--on-surface) 6%, transparent);
	}
	.cobros-period-option.is-selected {
		border-color: transparent;
		background: color-mix(in srgb, var(--primary) 12%, transparent);
		color: var(--primary);
	}
	.cobros-period-option.is-selected:hover {
		background: color-mix(in srgb, var(--primary) 18%, transparent);
	}
	.cobros-period-option .material-symbols-rounded {
		font-size: 1.2rem;
		opacity: 0;
	}
	.cobros-period-option.is-selected .material-symbols-rounded {
		opacity: 1;
	}
	.cobros-period-custom {
		display: grid;
		gap: 0.75rem;
		padding-top: 0.25rem;
		border-top: 1px solid var(--shell-border);
	}
	.cobros-period-custom.hidden {
		display: none;
	}
	.cobros-date-field {
		position: relative;
		display: block;
	}
	.cobros-date-field__text {
		width: 100%;
		border-radius: 1rem;
		border: 1px solid var(--shell-border);
		background: var(--surface-bright);
		padding: 0.75rem 2.75rem 0.75rem 0.95rem;
		font-size: 0.95rem;
		font-weight: 700;
		font-variant-numeric: tabular-nums;
		letter-spacing: 0.02em;
		color: var(--on-surface);
		outline: none;
		transition: border-color 140ms ease, box-shadow 140ms ease;
	}
	.cobros-date-field__text::placeholder {
		font-weight: 600;
		letter-spacing: 0.04em;
		color: color-mix(in srgb, var(--on-surface-variant) 78%, transparent);
	}
	.cobros-date-field__text:hover {
		border-color: color-mix(in srgb, var(--on-surface-variant) 55%, var(--shell-border));
	}
	.cobros-date-field__text:focus {
		border-color: var(--primary);
		box-shadow: 0 0 0 4px color-mix(in srgb, var(--primary) 14%, transparent);
	}
	.cobros-date-field__text.is-invalid {
		border-color: color-mix(in srgb, #f43f5e 55%, var(--shell-border));
	}
	.cobros-dp-foot-row {
		justify-content: space-between;
		width: 100%;
	}
	.cobros-date-field__pick {
		position: absolute;
		top: 50%;
		right: 0.35rem;
		transform: translateY(-50%);
		display: grid;
		place-items: center;
		width: 2.25rem;
		height: 2.25rem;
		border: 0;
		border-radius: 0.75rem;
		background: transparent;
		color: var(--on-surface-variant);
		cursor: pointer;
	}
	.cobros-date-field__pick:hover {
		background: color-mix(in srgb, var(--surface-variant) 60%, transparent);
		color: var(--on-surface);
	}
	.cobros-date-field__pick .material-symbols-rounded {
		font-size: 1.2rem;
	}
	html[data-theme='dark'] .cobros-date-field__text {
		background: var(--surface-container, #1e1f22);
		border-color: rgba(255, 255, 255, 0.16);
	}
	html[data-theme='dark'] .cobros-date-field__text:hover {
		border-color: rgba(255, 255, 255, 0.26);
	}
	.cobros-period-apply {
		display: inline-flex;
		width: 100%;
		align-items: center;
		justify-content: center;
		min-height: 2.75rem;
		border-radius: 999px;
		border: 1px solid color-mix(in srgb, var(--primary) 72%, #123e7a 28%);
		background: var(--primary);
		font-size: 0.92rem;
		font-weight: 780;
		color: #f8fbff;
		cursor: pointer;
	}
	@media (min-width: 640px) {
		.cobros-period-filter-sheet:not(.is-desktop-popover) {
			inset: 0;
			margin: auto;
			width: min(24rem, calc(100vw - 2rem));
			max-height: min(70vh, 26rem);
			height: fit-content;
			border-bottom: 1px solid var(--shell-border);
			border-radius: 1.6rem;
			background: var(--surface-bright);
			background-image: none;
			box-shadow: 0 8px 28px rgb(0 0 0 / 0.28);
		}
		.cobros-period-filter-sheet:not(.is-desktop-popover) .cobros-period-filter-sheet__handle {
			display: none;
		}
		.cobros-period-filter-sheet:not(.is-desktop-popover) .cobros-period-filter-sheet__header {
			padding: 1rem 1rem 0.85rem;
		}
	}
	@media (min-width: 768px) {
		.cobros-period-filter-sheet.is-desktop-popover {
			border: 1px solid var(--shell-border);
			border-radius: 1rem;
			overflow: hidden;
		}
	}
	:global(.cobros-chip) {
		display: inline-flex;
		align-items: center;
		border-radius: 999px;
		padding: 0.2rem 0.65rem;
		font-size: 0.75rem;
		font-weight: 800;
	}
	:global(.cobros-chip--pending) {
		background: #fef3c7;
		color: #92400e;
	}
	:global(.cobros-chip--approved) {
		background: #dcfce7;
		color: #166534;
	}
	:global(.cobros-chip--refund) {
		background: #dbeafe;
		color: #1e40af;
	}
	:global(.cobros-chip--sent) {
		background: #e5e7eb;
		color: #374151;
	}
	:global(.cobros-chip--expired) {
		background: #ffe4e6;
		color: #be123c;
	}
	:global(.cobros-chip--other) {
		background: var(--control-surface);
		color: var(--on-surface-variant);
	}

	:global(html[data-theme='dark'] .cobros-chip--expired) {
		background: color-mix(in srgb, #f43f5e 16%, transparent);
		color: #fb7185;
	}

	:global(.cobros-view-btn) {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2.35rem;
		height: 2.35rem;
		border: 0;
		border-radius: 0.75rem;
		background: color-mix(in srgb, var(--primary) 10%, transparent);
		color: var(--primary);
		cursor: pointer;
		transition: background 140ms ease, color 140ms ease;
	}
	:global(.cobros-view-btn:hover) {
		background: color-mix(in srgb, var(--primary) 18%, transparent);
	}
	:global(.cobros-view-btn .material-symbols-rounded) {
		font-size: 1.25rem;
		line-height: 1;
		pointer-events: none;
	}

	.cobros-modal-actions {
		display: flex;
		flex-direction: column-reverse;
		gap: 0.65rem;
		width: 100%;
	}

	.cobros-modal-actions.hidden {
		display: none;
	}

	@media (min-width: 640px) {
		.cobros-modal-actions {
			flex-direction: row;
			justify-content: flex-end;
			align-items: center;
			gap: 0.75rem;
		}
	}

	.cobros-modal-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3rem;
		width: 100%;
		border-radius: 999px;
		padding: 0 1.25rem;
		font-size: 1rem;
		font-weight: 700;
		cursor: pointer;
		border: 1px solid transparent;
		transition: background 140ms ease, border-color 140ms ease, color 140ms ease;
	}

	@media (min-width: 640px) {
		.cobros-modal-btn {
			min-width: 9.5rem;
			width: auto;
		}
	}

	.cobros-modal-btn--primary {
		background: var(--primary);
		color: var(--on-primary);
	}

	.cobros-modal-btn--ghost {
		background: transparent;
		border-color: color-mix(in srgb, #f43f5e 45%, var(--shell-border));
		color: #e11d48;
	}

	.cobros-modal-btn--muted {
		background: transparent;
		border-color: var(--shell-border);
		color: var(--on-surface-variant);
	}

	.cobros-modal-btn--danger {
		background: #be123c;
		border-color: transparent;
		color: #fff;
	}
	.cobros-modal-btn--danger:hover {
		background: #9f1239;
	}

	.cobros-reject-reveal {
		max-height: 0;
		opacity: 0;
		overflow: hidden;
		transition:
			max-height 200ms ease,
			opacity 200ms ease;
	}
	.cobros-reject-reveal.is-open {
		max-height: 14rem;
		opacity: 1;
	}

	.cobros-reject-alert {
		display: flex;
		align-items: flex-start;
		gap: 0.65rem;
		margin-bottom: 0.85rem;
		padding: 0.75rem 0.9rem;
		border-radius: 1rem;
		border: 1px solid color-mix(in srgb, #f59e0b 35%, transparent);
		background: color-mix(in srgb, #f59e0b 14%, transparent);
		color: #fbbf24;
	}
	.cobros-reject-alert__icon {
		flex-shrink: 0;
		font-size: 1.25rem;
		line-height: 1.2;
		color: #fbbf24;
	}
	.cobros-reject-alert__text {
		margin: 0;
		font-size: 0.86rem;
		font-weight: 650;
		line-height: 1.35;
		color: #fcd34d;
	}

	.cobros-receipt-row {
		align-self: start;
		width: 100%;
		height: fit-content;
	}
	.cobros-receipt-row.hidden {
		display: none;
	}
	.cobros-receipt-open {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2.25rem;
		height: 2.25rem;
		border: 0;
		border-radius: 999px;
		background: color-mix(in srgb, var(--primary) 12%, transparent);
		color: var(--primary);
		cursor: pointer;
		transition: background 140ms ease;
	}
	.cobros-receipt-open:hover {
		background: color-mix(in srgb, var(--primary) 20%, transparent);
	}
	.cobros-receipt-open .material-symbols-rounded {
		font-size: 1.2rem;
		line-height: 1;
		pointer-events: none;
	}

	.cobros-modal-footer [data-cobros-modal-status]:empty {
		display: none;
	}

	.cobros-modal-footer [data-cobros-modal-actions]:not(.hidden) + [data-cobros-modal-status]:not(:empty) {
		margin-top: 0.75rem;
	}

	/* Validar comprobante: sheet móvil → drawer desktop (mismo patrón que Clientes) */
	dialog[data-cobros-modal] {
		position: fixed;
		inset: 0;
		width: 100%;
		max-width: none;
		height: 100%;
		max-height: none;
		margin: 0;
		padding: 0;
		border: 0;
		background: transparent;
		box-shadow: none;
		overflow: hidden;
		animation: cobros-modal-in 180ms cubic-bezier(0.2, 0, 0, 1);
	}

	dialog[data-cobros-modal][open] {
		display: flex;
		flex-direction: column;
		align-items: stretch;
		justify-content: flex-end;
	}

	@media (min-width: 640px) {
		dialog[data-cobros-modal][open] {
			align-items: center;
			justify-content: center;
		}
	}

	dialog[data-cobros-modal]::backdrop {
		background: rgb(0 0 0 / 0.4);
		animation: cobros-backdrop-in 180ms ease;
	}

	dialog[data-cobros-modal].is-closing {
		animation: cobros-modal-out 140ms ease forwards;
	}

	dialog[data-cobros-modal].is-closing::backdrop {
		animation: cobros-backdrop-out 140ms ease forwards;
	}

	@keyframes cobros-modal-in {
		from {
			opacity: 0;
			transform: translateY(100%);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes cobros-modal-out {
		from {
			opacity: 1;
			transform: translateY(0);
		}
		to {
			opacity: 0;
			transform: translateY(100%);
		}
	}

	@keyframes cobros-backdrop-in {
		from {
			opacity: 0;
			backdrop-filter: blur(0);
			-webkit-backdrop-filter: blur(0);
		}
		to {
			opacity: 1;
			backdrop-filter: blur(var(--modal-backdrop-blur));
			-webkit-backdrop-filter: blur(var(--modal-backdrop-blur));
		}
	}

	@keyframes cobros-backdrop-out {
		from {
			opacity: 1;
			backdrop-filter: blur(var(--modal-backdrop-blur));
			-webkit-backdrop-filter: blur(var(--modal-backdrop-blur));
		}
		to {
			opacity: 0;
			backdrop-filter: blur(0);
			-webkit-backdrop-filter: blur(0);
		}
	}

	@media (min-width: 640px) and (max-width: 1023px) {
		@keyframes cobros-modal-in {
			from {
				opacity: 0;
				transform: translateY(12px) scale(0.98);
			}
			to {
				opacity: 1;
				transform: translateY(0) scale(1);
			}
		}

		@keyframes cobros-modal-out {
			from {
				opacity: 1;
				transform: translateY(0) scale(1);
			}
			to {
				opacity: 0;
				transform: translateY(8px) scale(0.98);
			}
		}
	}

	@media (min-width: 1024px) {
		dialog[data-cobros-modal][open] {
			flex-direction: row;
			align-items: stretch;
			justify-content: flex-end;
		}

		dialog[data-cobros-modal][open] .cobros-modal__panel {
			min-height: 100dvh;
			margin-left: auto;
		}

		dialog[data-cobros-modal] {
			animation-name: cobros-drawer-in;
		}

		dialog[data-cobros-modal].is-closing {
			animation-name: cobros-drawer-out;
		}

		/* Glass: blur + velo liviano (no negro sólido) */
		dialog[data-cobros-modal]::backdrop {
			background: var(--modal-backdrop-tint);
			backdrop-filter: blur(var(--modal-backdrop-blur));
			-webkit-backdrop-filter: blur(var(--modal-backdrop-blur));
		}
	}

	@keyframes cobros-drawer-in {
		from {
			opacity: 0;
			transform: translateX(100%);
		}
		to {
			opacity: 1;
			transform: translateX(0);
		}
	}

	@keyframes cobros-drawer-out {
		from {
			opacity: 1;
			transform: translateX(0);
		}
		to {
			opacity: 0;
			transform: translateX(100%);
		}
	}

	/* Visor de comprobante a pantalla completa (PDF/imagen) */
	.cobros-viewer {
		margin: 0;
		padding: 0;
		border: 0;
		width: 100%;
		max-width: 100%;
		height: 100%;
		max-height: 100%;
		background: var(--surface);
		color: var(--on-surface);
		overflow: hidden;
	}
	.cobros-viewer:not([open]) {
		display: none;
	}
	.cobros-viewer::backdrop {
		background: rgb(0 0 0 / 0.6);
	}
	.cobros-viewer[open] {
		display: flex;
		flex-direction: column;
	}
	.cobros-viewer__bar {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.75rem 1rem;
		border-bottom: 1px solid var(--shell-border);
		background: var(--surface-bright);
	}
	.cobros-viewer__name {
		margin: 0;
		flex: 1 1 auto;
		min-width: 0;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
		font-size: 0.95rem;
		font-weight: 700;
	}
	.cobros-viewer__open {
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
		height: 2.25rem;
		padding: 0 0.9rem;
		border-radius: 999px;
		background: var(--primary-container);
		color: var(--on-primary-container);
		font-size: 0.85rem;
		font-weight: 600;
		text-decoration: none;
	}
	.cobros-viewer__close {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		height: 2.25rem;
		width: 2.25rem;
		border: 0;
		border-radius: 999px;
		background: var(--control-surface);
		color: var(--on-surface);
		cursor: pointer;
	}
	.cobros-viewer__body {
		flex: 1 1 auto;
		min-height: 0;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgb(0 0 0 / 0.04);
		overflow: auto;
	}
	.cobros-viewer__img {
		max-width: 100%;
		max-height: 100%;
		object-fit: contain;
	}
	.cobros-viewer__frame {
		width: 100%;
		height: 100%;
		border: 0;
	}

	@media (min-width: 768px) {
		.cobros-viewer {
			width: min(880px, 92vw);
			height: min(90vh, 900px);
			max-height: 90vh;
			margin: auto;
			border-radius: 1.25rem;
			border: 1px solid var(--shell-border);
			box-shadow: 0 24px 60px rgb(0 0 0 / 0.25);
		}
		.cobros-viewer::backdrop {
			background: rgb(0 0 0 / 0.4);
			backdrop-filter: blur(6px);
			-webkit-backdrop-filter: blur(6px);
		}
	}
</style>

<script>
	import { initCobrosPage } from '../../scripts/cobros-page';

	const bootCobrosPage = () => {
		initCobrosPage();
	};

	bootCobrosPage();
	document.addEventListener('astro:page-load', bootCobrosPage);
</script>
