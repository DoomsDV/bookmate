---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
const siteOrigin = String(Astro.site ?? Astro.url.origin).replace(/\/+$/, '');
const pageTitle = 'Hasel - Software de citas, agenda y reservas online para tu negocio';
const metaDescription =
	'Organizá citas, equipo y clientes en un solo panel. Tus clientes reservan 24/7 con tu enlace personalizado. Probá Premium gratis 14 días.';

const usefulServices = [
	{
		icon: 'event_available',
		title: 'Citas automáticas',
		text: 'Tus clientes reservan 24/7. Recordatorios automáticos para reducir cancelaciones.',
		variant: 'featured',
		image: '/landing/salon.jpg',
		imageAlt: 'Recepción de salón con agenda digital',
	},
	{
		icon: 'show_chart',
		title: 'Métricas claras',
		text: 'Ingresos, ticket promedio y tendencias en números simples para decidir con datos.',
		variant: 'tint',
	},
	{
		icon: 'apartment',
		title: 'Equipo y sucursales',
		text: 'Disponibilidad en tiempo real, turnos y varias ubicaciones desde un solo panel.',
		variant: 'default',
	},
	{
		icon: 'desktop_windows',
		title: 'Página de reservas',
		text: 'Link propio con disponibilidad real. Tus clientes eligen horario sin llamarte.',
		variant: 'visual',
		image: '/landing/barber.jpg',
		imageAlt: 'Interior de barbería con espejo y sillón',
	},
] as const;

const steps = [
	{
		icon: 'tune',
		title: 'Configurá servicios y horarios',
		text: 'Definí tu catálogo, profesionales y disponibilidad en minutos.',
	},
	{
		icon: 'share',
		title: 'Compartí tu enlace',
		text: 'Pegalo en WhatsApp, Instagram o tu bio. Reservas sin ida y vuelta.',
	},
	{
		icon: 'calendar_month',
		title: 'Controlá desde el calendario',
		text: 'Todo se actualiza solo. Hasel envía recordatorios por vos.',
	},
];

const jsonLd = [
	{
		'@context': 'https://schema.org',
		'@type': 'WebSite',
		name: 'Hasel',
		url: siteOrigin,
		description: metaDescription,
		inLanguage: 'es',
	},
	{
		'@context': 'https://schema.org',
		'@type': 'SoftwareApplication',
		name: 'Hasel',
		applicationCategory: 'BusinessApplication',
		operatingSystem: 'Web',
		url: siteOrigin,
		description: metaDescription,
		offers: {
			'@type': 'Offer',
			price: '0',
			priceCurrency: 'PYG',
			description: 'Prueba Premium 14 días sin tarjeta',
		},
		featureList: [
			...usefulServices.map((service) => service.title),
			'Prueba Premium 14 días',
			...steps.map((step) => step.title),
		].join(', '),
	},
];
---

<Layout
	pageTitle={pageTitle}
	metaDescription={metaDescription}
	canonicalPath="/"
	ogImage="/icons/icon-512x512.png"
	jsonLd={jsonLd}
>
	<link
		slot="head"
		rel="preload"
		as="image"
		type="image/avif"
		href="/calendar/desktop/calendario-light.avif"
		fetchpriority="high"
	/>
	<link slot="head" rel="preload" as="image" type="image/webp" href="/calendar/desktop/calendario-light.webp" />
	<main class="landing-root">
		<a href="#hero-heading" class="landing-skip-link">Saltar al contenido principal</a>
		<div class="landing-scroll-sentinel" data-scroll-sentinel aria-hidden="true"></div>

		<header class="landing-topbar" data-landing-topbar>
			<div class="landing-topbar-inner">
				<a href="/" class="brand no-underline" aria-label="Hasel - Inicio">
					<img
						src="/icons/icon-64.png"
						alt=""
						class="brand-logo"
						width="64"
						height="64"
						loading="eager"
						decoding="async"
					/>
					<span class="brand-text">Hasel</span>
				</a>

				<nav class="landing-topbar-links" aria-label="Secciones">
					<a href="#funciones" class="landing-topbar-link no-underline">Funciones</a>
					<a href="#como-funciona" class="landing-topbar-link no-underline">Cómo funciona</a>
					<a href="#precios" class="landing-topbar-link no-underline">Precios</a>
					<a href="#soporte" class="landing-topbar-link no-underline">Soporte</a>
				</nav>

				<div class="nav-actions">
					<a href="/auth/login" class="m3-btn m3-btn-text m3-btn-nav no-underline">Iniciar sesión</a>
					<a href="/auth/register" class="m3-btn m3-btn-filled m3-btn-nav no-underline">Empezar gratis</a>
				</div>
			</div>
		</header>

		<div class="landing-shell">
			<section class="m3-hero" aria-labelledby="hero-heading">
				<div class="m3-hero-content">
					<p class="m3-chip m3-chip-primary">
						<span class="material-symbols-rounded" aria-hidden="true">workspace_premium</span>
						<span>14 días Premium de prueba</span>
					</p>
					<h1 id="hero-heading" class="m3-display" data-hero-reveal data-hero-reveal-delay="0">
						Tu negocio organizado.<br />Tus clientes agendando solos.
					</h1>
					<p class="m3-body-large" data-hero-reveal data-hero-reveal-delay="120">
						Calendario, equipo y link de reservas para WhatsApp. Todo en un solo panel.
					</p>

					<div class="hero-actions" data-hero-reveal data-hero-reveal-delay="180">
						<a href="/auth/register" class="m3-btn m3-btn-filled m3-btn-large no-underline">
							Empezar gratis
						</a>
						<a href="#funciones" class="m3-btn m3-btn-tonal m3-btn-large no-underline">Ver funciones</a>
					</div>
				</div>

				<div class="hero-visual" data-hero-reveal data-hero-reveal-delay="220">
					<div class="hero-monitor">
						<div class="hero-monitor__bezel">
							<div class="hero-monitor__screen">
								<div
									class="hero-calendar-split"
									data-calendar-compare
									style="--split: 50%; --skew: 14%"
									aria-label="Compará el calendario en modo claro y oscuro. Deslizá para revelar."
								>
									<div class="hero-calendar-split__pane hero-calendar-split__pane--light">
										<picture>
											<source type="image/avif" srcset="/calendar/desktop/calendario-light.avif" />
											<source type="image/webp" srcset="/calendar/desktop/calendario-light.webp" />
											<img
												src="/calendar/desktop/calendario-light.png"
												alt="Calendario Hasel en modo claro"
												width="1883"
												height="957"
												loading="eager"
												decoding="async"
												draggable="false"
											/>
										</picture>
									</div>
									<div class="hero-calendar-split__pane hero-calendar-split__pane--dark">
										<picture>
											<source type="image/avif" srcset="/calendar/desktop/calendario-dark.avif" />
											<source type="image/webp" srcset="/calendar/desktop/calendario-dark.webp" />
											<img
												src="/calendar/desktop/calendario-dark.png"
												alt="Calendario Hasel en modo oscuro"
												width="1879"
												height="961"
												loading="eager"
												decoding="async"
												draggable="false"
											/>
										</picture>
									</div>
									<div
										class="hero-calendar-split__handle"
										data-calendar-handle
										role="slider"
										tabindex="0"
										aria-orientation="horizontal"
										aria-valuemin="0"
										aria-valuemax="100"
										aria-valuenow="50"
										aria-label="Deslizá para comparar modo claro y oscuro"
									>
										<span class="hero-calendar-split__handle-line" aria-hidden="true"></span>
										<span class="hero-calendar-split__handle-knob material-symbols-rounded" aria-hidden="true"
											>compare_arrows</span
										>
									</div>
									<span class="hero-calendar-split__label hero-calendar-split__label--light">Claro</span>
									<span class="hero-calendar-split__label hero-calendar-split__label--dark">Oscuro</span>
								</div>
							</div>
						</div>
						<div class="hero-monitor__stand" aria-hidden="true">
							<span class="hero-monitor__neck"></span>
							<span class="hero-monitor__base"></span>
						</div>
					</div>

					<div class="hero-phone">
						<div class="hero-phone__bezel">
							<span class="hero-phone__island" aria-hidden="true"></span>
							<div class="hero-phone__screen">
								<div
									class="hero-calendar-split hero-calendar-split--mobile"
									data-calendar-compare
									style="--split: 50%; --skew: 12%"
									aria-label="Compará el calendario móvil en modo claro y oscuro. Deslizá para revelar."
								>
									<div class="hero-calendar-split__pane hero-calendar-split__pane--light">
										<picture>
											<source type="image/avif" srcset="/calendar/mobile/calendario-light.avif" />
											<source type="image/webp" srcset="/calendar/mobile/calendario-light.webp" />
											<img
												src="/calendar/mobile/calendario-light.png"
												alt="Calendario Hasel móvil en modo claro"
												width="780"
												height="1691"
												loading="eager"
												decoding="async"
												draggable="false"
											/>
										</picture>
									</div>
									<div class="hero-calendar-split__pane hero-calendar-split__pane--dark">
										<picture>
											<source type="image/avif" srcset="/calendar/mobile/calendario-dark.avif" />
											<source type="image/webp" srcset="/calendar/mobile/calendario-dark.webp" />
											<img
												src="/calendar/mobile/calendario-dark.png"
												alt="Calendario Hasel móvil en modo oscuro"
												width="780"
												height="1691"
												loading="eager"
												decoding="async"
												draggable="false"
											/>
										</picture>
									</div>
									<div
										class="hero-calendar-split__handle"
										data-calendar-handle
										role="slider"
										tabindex="0"
										aria-orientation="horizontal"
										aria-valuemin="0"
										aria-valuemax="100"
										aria-valuenow="50"
										aria-label="Deslizá para comparar modo claro y oscuro"
									>
										<span class="hero-calendar-split__handle-line" aria-hidden="true"></span>
										<span class="hero-calendar-split__handle-knob material-symbols-rounded" aria-hidden="true"
											>compare_arrows</span
										>
									</div>
									<span class="hero-calendar-split__label hero-calendar-split__label--light">Claro</span>
									<span class="hero-calendar-split__label hero-calendar-split__label--dark">Oscuro</span>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<section id="funciones" class="m3-section features-section" aria-labelledby="funciones-heading">
				<div class="features-intro reveal-on-scroll">
					<h2 id="funciones-heading" class="m3-headline">
						Todo lo que necesitás para operar y crecer
					</h2>
					<p class="m3-body features-lead">
						Desde la primera reserva hasta el seguimiento del cliente, sin planillas ni mensajes perdidos.
					</p>
				</div>

				<div class="features-bento" data-features-bento>
					{
						usefulServices.map((service, index) => (
							<article
								class:list={[
									'features-bento__cell',
									`features-bento__cell--${service.variant}`,
									'reveal-on-scroll',
								]}
								style={`--reveal-i: ${index}`}
							>
								{'image' in service && service.image ? (
									<img
										class="features-bento__photo"
										src={service.image}
										alt={service.imageAlt}
										width={800}
										height={600}
										loading="lazy"
										decoding="async"
									/>
								) : null}
								<div class="features-bento__content">
									<span class="features-bento__icon" aria-hidden="true">
										<span class="material-symbols-rounded">{service.icon}</span>
									</span>
									<h3 class="features-bento__title">{service.title}</h3>
									<p class="features-bento__text">{service.text}</p>
								</div>
							</article>
						))
					}
				</div>
			</section>

			<section id="como-funciona" class="m3-section workflow-section" aria-labelledby="como-funciona-heading">
				<div class="workflow-intro reveal-on-scroll">
					<h2 id="como-funciona-heading" class="m3-headline">Cómo funciona Hasel</h2>
					<p class="m3-body workflow-lead">Tres pasos y ya estás recibiendo reservas.</p>
				</div>

				<ol class="workflow-rail">
					{
						steps.map((item) => (
							<li class="workflow-rail__step reveal-on-scroll">
								<span class="workflow-rail__icon material-symbols-rounded" aria-hidden="true">
									{item.icon}
								</span>
								<div class="workflow-rail__copy">
									<h3 class="m3-title">{item.title}</h3>
									<p class="m3-body">{item.text}</p>
								</div>
							</li>
						))
					}
				</ol>
			</section>

			<section id="precios" class="m3-cta-split" aria-labelledby="cta-heading">
				<div class="cta-split__copy reveal-on-scroll">
					<h2 id="cta-heading" class="m3-headline">
						Probá <span class="cta-premium">Premium</span> 14 días, sin tarjeta
					</h2>
					<p class="m3-body-large">
						Creá tu espacio, configurá horarios y compartí tu página pública hoy mismo.
					</p>
				</div>
				<div class="cta-split__panel reveal-on-scroll">
					<ul class="cta-checklist">
						<li>
							<span class="material-symbols-rounded" aria-hidden="true">check_circle</span>
							Calendario compartido con tu equipo
						</li>
						<li>
							<span class="material-symbols-rounded" aria-hidden="true">check_circle</span>
							Página pública de reservas
						</li>
						<li>
							<span class="material-symbols-rounded" aria-hidden="true">check_circle</span>
							Recordatorios automáticos
						</li>
					</ul>
					<a href="/auth/register" class="m3-btn m3-btn-filled m3-btn-large no-underline cta-btn">
						Empezar gratis
						<span class="material-symbols-rounded" aria-hidden="true">arrow_forward</span>
					</a>
				</div>
			</section>

			<footer id="soporte" class="m3-footer">
				<div class="m3-footer-brand">
					<p class="m3-footer-copy">© {new Date().getFullYear()} Hasel</p>
					<p class="m3-footer-tagline">Software de reservas</p>
				</div>
				<div class="m3-footer-links">
					<p class="m3-footer-support">
						<span>¿Necesitás ayuda? Escribinos al correo</span>
						<a href="mailto:support@hasel.app" class="m3-footer-link no-underline">support@hasel.app</a>
					</p>
					<nav class="m3-footer-nav" aria-label="Enlaces legales">
						<a href="/politicas-y-privacidad" class="m3-footer-link no-underline">
							Políticas y privacidad
						</a>
						<a href="/politicas-de-cancelacion-y-reembolso" class="m3-footer-link no-underline">
							Cancelación y reembolso
						</a>
					</nav>
				</div>
			</footer>
		</div>
	</main>
</Layout>

<script is:inline>
	(() => {
		if (typeof window === 'undefined') return;

		/** @type {AbortController | null} */
		let landingAbort = null;
		/** @type {ReturnType<typeof setTimeout>[]} */
		let landingTimeouts = [];

		const clearLandingTimeouts = () => {
			for (const id of landingTimeouts) window.clearTimeout(id);
			landingTimeouts = [];
		};

		const later = (fn, ms) => {
			const id = window.setTimeout(fn, ms);
			landingTimeouts.push(id);
			return id;
		};

		const initLandingPage = () => {
			const root = document.querySelector('.landing-root');
			if (!(root instanceof HTMLElement)) return;

			landingAbort?.abort();
			clearLandingTimeouts();
			landingAbort = new AbortController();
			const { signal } = landingAbort;

			const nav = document.querySelector('[data-landing-topbar]');
			const sentinel = document.querySelector('[data-scroll-sentinel]');
			const getOffset = () => {
				const navHeight = nav instanceof HTMLElement ? nav.getBoundingClientRect().height : 0;
				return navHeight + 12;
			};

			if (nav instanceof HTMLElement && sentinel instanceof HTMLElement && 'IntersectionObserver' in window) {
				const topObserver = new IntersectionObserver(
					([entry]) => {
						if (!entry) return;
						nav.classList.toggle('is-scrolled', !entry.isIntersecting);
					},
					{ root: null, threshold: 0, rootMargin: '-1px 0px 0px 0px' }
				);
				topObserver.observe(sentinel);
				signal.addEventListener('abort', () => topObserver.disconnect());
			}

			const scrollToHash = (hash, behavior = 'smooth') => {
				if (!hash || !hash.startsWith('#')) return;
				const id = decodeURIComponent(hash.slice(1));
				if (!id) return;
				const target = document.getElementById(id);
				if (!target) return;

				const top = target.getBoundingClientRect().top + window.scrollY - getOffset();
				window.scrollTo({ top: Math.max(0, top), behavior });
			};

			root.querySelectorAll('a[href^="#"]').forEach((anchor) => {
				anchor.addEventListener(
					'click',
					(event) => {
						const href = anchor.getAttribute('href');
						if (!href) return;
						event.preventDefault();
						history.pushState(null, '', href);
						scrollToHash(href, 'smooth');
					},
					{ signal }
				);
			});

			if (window.location.hash) {
				requestAnimationFrame(() => scrollToHash(window.location.hash, 'auto'));
			}

			const heroRevealNodes = Array.from(root.querySelectorAll('[data-hero-reveal]')).filter(
				(node) => node instanceof HTMLElement
			);
			const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

			const clearHeroReveal = () => {
				heroRevealNodes.forEach((node) => {
					node.classList.remove('is-in');
					node.style.animation = 'none';
				});
			};

			const runHeroReveal = () => {
				if (!heroRevealNodes.length) return;

				if (prefersReducedMotion) {
					heroRevealNodes.forEach((node) => {
						node.style.animation = '';
						node.classList.add('is-in');
					});
					return;
				}

				clearHeroReveal();
				void document.body.offsetWidth;

				window.requestAnimationFrame(() => {
					window.requestAnimationFrame(() => {
						heroRevealNodes.forEach((node) => {
							node.style.animation = '';
							const delay = Number(node.getAttribute('data-hero-reveal-delay') || 0);
							const safeDelay = Number.isFinite(delay) ? Math.max(0, delay) : 0;
							later(() => node.classList.add('is-in'), safeDelay + 40);
						});
					});
				});
			};

			const prepareHeroFonts = async () => {
				if (!document.fonts?.load) return;
				try {
					await Promise.race([
						Promise.all([
							document.fonts.load('800 3.5rem "Nunito Sans"'),
							document.fonts.load('400 1.125rem "Nunito Sans"'),
							document.fonts.load('700 1rem "Nunito Sans"'),
						]),
						new Promise((resolve) => window.setTimeout(resolve, 300)),
					]);
				} catch {
					/* ignore */
				}
			};

			if (heroRevealNodes.length) {
				void prepareHeroFonts().then(runHeroReveal);
			}

			const revealNodes = root.querySelectorAll('.reveal-on-scroll');
			if (revealNodes.length && 'IntersectionObserver' in window) {
				const revealObserver = new IntersectionObserver(
					(entries) => {
						for (const entry of entries) {
							if (!entry.isIntersecting) continue;
							entry.target.classList.add('is-visible');
							revealObserver.unobserve(entry.target);
						}
					},
					{ rootMargin: '0px 0px -10% 0px', threshold: 0.15 }
				);
				revealNodes.forEach((node) => revealObserver.observe(node));
				signal.addEventListener('abort', () => revealObserver.disconnect());
			} else {
				revealNodes.forEach((node) => node.classList.add('is-visible'));
			}

			root.querySelectorAll('[data-calendar-compare]').forEach((compareRoot) => {
				if (!(compareRoot instanceof HTMLElement)) return;
				const handle = compareRoot.querySelector('[data-calendar-handle]');
				const skewRaw = getComputedStyle(compareRoot).getPropertyValue('--skew').trim();
				const SKEW = Number.parseFloat(skewRaw) || 14;
				let split = 50;
				let dragging = false;

				const setSplit = (pct) => {
					split = Math.min(100, Math.max(0, pct));
					const splitTop = (split / 100) * (100 + SKEW);
					compareRoot.style.setProperty('--split', `${splitTop}%`);
					if (handle instanceof HTMLElement) {
						handle.setAttribute('aria-valuenow', String(Math.round(split)));
					}
				};

				const updateFromClientX = (clientX) => {
					const rect = compareRoot.getBoundingClientRect();
					if (rect.width <= 0) return;
					const pct = ((clientX - rect.left) / rect.width) * 100;
					setSplit(pct);
				};

				compareRoot.addEventListener(
					'pointerdown',
					(event) => {
						if (!(event instanceof PointerEvent)) return;
						dragging = true;
						compareRoot.classList.add('is-dragging');
						compareRoot.setPointerCapture?.(event.pointerId);
						updateFromClientX(event.clientX);
						event.preventDefault();
					},
					{ signal }
				);
				compareRoot.addEventListener(
					'pointermove',
					(event) => {
						if (!dragging || !(event instanceof PointerEvent)) return;
						updateFromClientX(event.clientX);
					},
					{ signal }
				);
				const onPointerUp = (event) => {
					if (!dragging || !(event instanceof PointerEvent)) return;
					dragging = false;
					compareRoot.classList.remove('is-dragging');
					try {
						compareRoot.releasePointerCapture?.(event.pointerId);
					} catch {
						/* ignore */
					}
				};
				compareRoot.addEventListener('pointerup', onPointerUp, { signal });
				compareRoot.addEventListener('pointercancel', onPointerUp, { signal });
				compareRoot.addEventListener(
					'lostpointercapture',
					() => {
						dragging = false;
						compareRoot.classList.remove('is-dragging');
					},
					{ signal }
				);

				if (handle instanceof HTMLElement) {
					handle.addEventListener(
						'keydown',
						(event) => {
							if (!(event instanceof KeyboardEvent)) return;
							const step = event.shiftKey ? 10 : 3;
							if (event.key === 'ArrowLeft' || event.key === 'ArrowDown') {
								event.preventDefault();
								setSplit(split - step);
							} else if (event.key === 'ArrowRight' || event.key === 'ArrowUp') {
								event.preventDefault();
								setSplit(split + step);
							} else if (event.key === 'Home') {
								event.preventDefault();
								setSplit(0);
							} else if (event.key === 'End') {
								event.preventDefault();
								setSplit(100);
							}
						},
						{ signal }
					);
				}

				setSplit(50);
			});
		};

		if (!window.__haselLandingPageBound) {
			window.__haselLandingPageBound = true;
			document.addEventListener('astro:page-load', initLandingPage);
		}
		initLandingPage();
	})();
</script>

<style>
	.landing-root {
		--accent: #426edb;
		--accent-hover: #5a82e8;
		--accent-soft: rgba(66, 110, 219, 0.14);
		--accent-on: #ffffff;
		--surface: #0c0e14;
		--surface-raised: #141820;
		--surface-overlay: #1a2030;
		--text-primary: #eef0f4;
		--text-secondary: #a8adb8;
		--text-muted: #7a8190;
		--border: rgba(255, 255, 255, 0.08);
		--border-strong: rgba(255, 255, 255, 0.14);
		--radius-sm: 8px;
		--radius-md: 14px;
		--radius-lg: 20px;
		--radius-pill: 9999px;
		--ease: cubic-bezier(0.16, 1, 0.3, 1);
		--landing-max: 72rem;
		--landing-pad-x: 1.25rem;
		--landing-topbar-h: 4rem;

		position: relative;
		overflow-x: clip;
		min-height: 100dvh;
		background:
			radial-gradient(ellipse 120% 70% at 50% -15%, rgba(66, 110, 219, 0.07), transparent 55%),
			var(--surface);
		color: var(--text-primary);
		font-family: var(--font-body, 'Nunito Sans', system-ui, sans-serif);
		-webkit-font-smoothing: antialiased;
	}

	:global(html:has(.landing-root)),
	:global(body:has(.landing-root)) {
		overflow-x: clip;
		background: var(--surface);
		margin: 0;
		padding: 0;
	}

	:global(body:has(.landing-root)) {
		min-height: 0;
	}

	:global(html:has(.landing-root)) {
		scroll-padding-top: calc(var(--landing-topbar-h) + 0.75rem);
	}

	.landing-root [id]:not(#soporte) {
		scroll-margin-top: calc(var(--landing-topbar-h) + 0.75rem);
	}

	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	.landing-skip-link {
		position: fixed;
		top: 0.65rem;
		left: 0.65rem;
		z-index: 100;
		padding: 0.55rem 0.9rem;
		border-radius: var(--radius-sm);
		background: var(--accent);
		color: var(--accent-on);
		font-size: 0.8125rem;
		font-weight: 700;
		text-decoration: none;
		transform: translateY(-160%);
		opacity: 0;
		pointer-events: none;
		transition:
			transform 200ms var(--ease),
			opacity 200ms var(--ease);
	}

	.landing-skip-link:focus {
		transform: translateY(0);
		opacity: 1;
		pointer-events: auto;
		outline: 2px solid #fff;
		outline-offset: 2px;
	}

	.landing-scroll-sentinel {
		position: absolute;
		top: 0;
		left: 0;
		width: 1px;
		height: 1px;
		pointer-events: none;
	}

	@media (min-width: 768px) {
		.landing-root {
			--landing-pad-x: 2rem;
		}
	}

	@media (min-width: 1024px) {
		.landing-root {
			--landing-pad-x: 2.5rem;
		}
	}

	.landing-shell {
		position: relative;
		z-index: 1;
		width: min(var(--landing-max), 100%);
		margin: 0 auto;
		padding: 0 var(--landing-pad-x) 0;
		display: flex;
		flex-direction: column;
		gap: 0;
	}

	/* Top bar */
	.landing-topbar {
		position: sticky;
		top: 0;
		z-index: 40;
		width: 100%;
		padding: 0.65rem var(--landing-pad-x);
		background: color-mix(in srgb, var(--surface) 80%, transparent);
		backdrop-filter: blur(14px);
		-webkit-backdrop-filter: blur(14px);
		border-bottom: 1px solid transparent;
		transition:
			background-color 200ms var(--ease),
			border-color 200ms var(--ease);
	}

	.landing-topbar.is-scrolled {
		border-bottom-color: var(--border);
		background: color-mix(in srgb, var(--surface) 92%, transparent);
	}

	@media (prefers-reduced-transparency: reduce) {
		.landing-topbar {
			background: var(--surface);
			backdrop-filter: none;
			-webkit-backdrop-filter: none;
		}

		.landing-topbar.is-scrolled {
			background: var(--surface);
		}
	}

	.landing-topbar-inner {
		width: min(var(--landing-max), 100%);
		margin: 0 auto;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
		min-height: 2.75rem;
	}

	.landing-topbar-links {
		display: none;
		align-items: center;
		gap: 0.1rem;
	}

	.landing-topbar-link {
		color: var(--text-secondary);
		font-size: 0.875rem;
		font-weight: 600;
		padding: 0.45rem 0.75rem;
		border-radius: var(--radius-pill);
		white-space: nowrap;
		transition:
			color 200ms var(--ease),
			background-color 200ms var(--ease);
	}

	.landing-topbar-link:hover {
		color: var(--text-primary);
		background: rgba(255, 255, 255, 0.06);
	}

	.landing-topbar-link:focus-visible,
	.brand:focus-visible,
	.m3-footer-link:focus-visible {
		outline: 2px solid var(--accent);
		outline-offset: 3px;
	}

	.brand {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		color: var(--text-primary);
		flex-shrink: 0;
	}

	.brand-logo {
		height: 2rem;
		width: 2rem;
		border-radius: var(--radius-pill);
		object-fit: contain;
	}

	.brand-text {
		font-weight: 800;
		font-size: 1.15rem;
		letter-spacing: -0.02em;
	}

	.nav-actions {
		display: flex;
		align-items: center;
		gap: 0.2rem;
		flex-shrink: 0;
	}

	@media (min-width: 1024px) {
		.landing-topbar-inner {
			display: grid;
			grid-template-columns: auto 1fr auto;
			align-items: center;
		}

		.landing-topbar-links {
			display: flex;
			justify-self: center;
		}
	}

	/* Buttons */
	.m3-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.45rem;
		min-height: 2.5rem;
		padding: 0.6rem 1.35rem;
		border-radius: var(--radius-pill);
		border: 1px solid transparent;
		font-family: inherit;
		font-size: 0.875rem;
		font-weight: 700;
		line-height: 1.25;
		text-decoration: none;
		cursor: pointer;
		white-space: nowrap;
		transition:
			background-color 200ms var(--ease),
			transform 200ms var(--ease),
			box-shadow 200ms var(--ease);
	}

	.m3-btn:hover {
		transform: translateY(-1px);
	}

	.m3-btn:active {
		transform: scale(0.98);
	}

	.m3-btn:focus-visible {
		outline: 2px solid var(--accent);
		outline-offset: 3px;
	}

	.m3-btn-filled:focus-visible {
		outline-color: #fff;
	}

	.m3-btn-large {
		min-height: 3rem;
		padding: 0.8rem 1.65rem;
		font-size: 0.9375rem;
	}

	.m3-btn-nav {
		min-height: 2.35rem;
		padding: 0.45rem 1rem;
		font-size: 0.8125rem;
	}

	.m3-btn-filled {
		background: var(--accent);
		color: var(--accent-on);
		box-shadow: 0 1px 2px rgba(12, 20, 40, 0.35);
	}

	.m3-btn-filled:hover {
		background: var(--accent-hover);
	}

	.m3-btn-tonal {
		background: var(--surface-overlay);
		color: var(--text-primary);
		border-color: var(--border);
	}

	.m3-btn-tonal:hover {
		background: color-mix(in srgb, var(--surface-overlay) 85%, white 15%);
	}

	.m3-btn-text {
		background: transparent;
		color: var(--text-secondary);
		box-shadow: none;
	}

	.m3-btn-text:hover {
		color: var(--text-primary);
		background: rgba(255, 255, 255, 0.06);
		transform: none;
	}

	@media (max-width: 480px) {
		.nav-actions .m3-btn-text {
			padding-inline: 0.5rem;
			font-size: 0.78rem;
		}

		.nav-actions .m3-btn-filled {
			padding-inline: 0.8rem;
			font-size: 0.78rem;
		}
	}

	/* Chips */
	.m3-chip {
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
		margin: 0;
		padding: 0.35rem 0.8rem 0.35rem 0.6rem;
		border-radius: var(--radius-pill);
		font-size: 0.8125rem;
		font-weight: 700;
		line-height: 1.2;
	}

	.m3-chip-primary {
		background: var(--accent-soft);
		color: #c5d4ff;
		border: 1px solid rgba(66, 110, 219, 0.35);
	}

	.m3-chip .material-symbols-rounded {
		font-size: 1rem;
	}

	/* Type */
	.m3-display {
		margin: 0;
		font-size: clamp(2rem, 5.5vw, 3.35rem);
		font-weight: 800;
		line-height: 1.12;
		letter-spacing: -0.03em;
		color: var(--text-primary);
		text-wrap: balance;
	}

	.m3-headline {
		margin: 0;
		font-size: clamp(1.65rem, 3.2vw, 2.35rem);
		font-weight: 800;
		letter-spacing: -0.025em;
		line-height: 1.15;
		color: var(--text-primary);
		text-wrap: balance;
	}

	.m3-title {
		margin: 0;
		font-size: 1.05rem;
		font-weight: 700;
		color: var(--text-primary);
	}

	.m3-body-large {
		margin: 0;
		max-width: 36rem;
		font-size: clamp(1rem, 1.5vw, 1.0625rem);
		line-height: 1.55;
		color: var(--text-secondary);
		text-wrap: pretty;
	}

	.m3-body {
		margin: 0;
		font-size: 0.9375rem;
		line-height: 1.55;
		color: var(--text-secondary);
		text-wrap: pretty;
	}

	@keyframes hero-text-enter {
		from {
			opacity: 0;
			transform: translateY(12px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	[data-hero-reveal] {
		opacity: 0;
	}

	[data-hero-reveal].is-in {
		opacity: 1;
		animation: hero-text-enter 0.7s var(--ease) both;
	}

	/* Hero */
	.m3-hero {
		display: grid;
		gap: 1.75rem;
		padding: 1.25rem 0 1.75rem;
		align-items: center;
	}

	.m3-hero-content {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 1.1rem;
		min-width: 0;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.65rem;
	}

	.hero-visual {
		width: 100%;
		min-width: 0;
	}

	.hero-monitor {
		display: none;
		flex-direction: column;
		align-items: center;
		width: 100%;
	}

	.hero-phone {
		display: flex;
		flex-direction: column;
		align-items: center;
		width: min(100%, 14rem);
		margin: 0 auto;
	}

	@media (max-width: 380px) {
		.hero-phone {
			width: min(100%, 12.5rem);
		}

		.m3-hero-content {
			gap: 0.95rem;
		}
	}

	.hero-phone__bezel,
	.hero-monitor__bezel {
		width: 100%;
		padding: 0.5rem;
		border-radius: 1.5rem;
		background: linear-gradient(160deg, #343842 0%, #181b22 55%, #101218 100%);
		border: 1px solid var(--border-strong);
		box-shadow:
			inset 0 1px 0 rgba(255, 255, 255, 0.07),
			0 20px 50px rgba(0, 0, 0, 0.45);
	}

	.hero-phone__bezel {
		position: relative;
		border-radius: 1.85rem;
	}

	.hero-phone__island {
		position: absolute;
		z-index: 6;
		top: 0.7rem;
		left: 50%;
		transform: translateX(-50%);
		width: 28%;
		height: 0.7rem;
		border-radius: var(--radius-pill);
		background: #08090c;
		pointer-events: none;
	}

	.hero-phone__screen,
	.hero-monitor__screen {
		position: relative;
		border-radius: 1rem;
		overflow: hidden;
		background: #08090c;
		box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.55) inset;
	}

	.hero-monitor__screen {
		border-radius: 0.75rem;
	}

	.hero-monitor__stand {
		display: flex;
		flex-direction: column;
		align-items: center;
		width: 100%;
		pointer-events: none;
	}

	.hero-monitor__neck {
		width: 16%;
		height: 1rem;
		background: linear-gradient(90deg, #181a20, #2a2e38 50%, #181a20);
		border-radius: 0 0 0.15rem 0.15rem;
	}

	.hero-monitor__base {
		width: 38%;
		height: 0.5rem;
		margin-top: -1px;
		border-radius: 999px 999px 40% 40%;
		background: linear-gradient(180deg, #2a2e38, #12141a);
		border: 1px solid var(--border);
	}

	.hero-calendar-split {
		--split: 50%;
		--skew: 14%;
		--split-angle: 12.5deg;
		position: relative;
		isolation: isolate;
		width: 100%;
		aspect-ratio: 16 / 10;
		overflow: hidden;
		background: var(--surface-raised);
		touch-action: none;
		cursor: ew-resize;
		user-select: none;
		-webkit-user-select: none;
	}

	.hero-calendar-split--mobile {
		--skew: 12%;
		--split-angle: 10deg;
		aspect-ratio: 780 / 1691;
	}

	.hero-calendar-split.is-dragging {
		cursor: grabbing;
	}

	.hero-calendar-split__pane {
		position: absolute;
		inset: 0;
	}

	.hero-calendar-split__pane picture {
		display: block;
		width: 100%;
		height: 100%;
	}

	.hero-calendar-split__pane img {
		display: block;
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: left top;
		pointer-events: none;
		user-select: none;
	}

	.hero-calendar-split__pane--light {
		z-index: 1;
		clip-path: polygon(
			0 0,
			calc(var(--split) + 0.65%) 0,
			calc(var(--split) - var(--skew) + 0.65%) 100%,
			0 100%
		);
	}

	.hero-calendar-split__pane--dark {
		z-index: 2;
		clip-path: polygon(
			calc(var(--split) - 0.65%) 0,
			100% 0,
			100% 100%,
			calc(var(--split) - var(--skew) - 0.65%) 100%
		);
	}

	.hero-calendar-split__handle {
		position: absolute;
		z-index: 5;
		top: 0;
		bottom: 0;
		left: calc(var(--split) - (var(--skew) * 0.5));
		width: 44px;
		transform: translateX(-50%) rotate(var(--split-angle));
		display: grid;
		place-items: center;
		cursor: ew-resize;
		outline: none;
	}

	.hero-calendar-split__handle-line {
		display: none;
	}

	.hero-calendar-split__handle-knob {
		display: grid;
		place-items: center;
		width: 2.25rem;
		height: 2.25rem;
		border-radius: var(--radius-pill);
		background: #fff;
		color: #1a1c22;
		font-size: 1.1rem;
		transform: rotate(-12.5deg);
		box-shadow: 0 4px 14px rgba(0, 0, 0, 0.35);
		pointer-events: none;
	}

	.hero-calendar-split__handle:focus-visible .hero-calendar-split__handle-knob {
		outline: 2px solid var(--accent);
		outline-offset: 3px;
	}

	.hero-calendar-split__label {
		position: absolute;
		z-index: 4;
		padding: 0.25rem 0.55rem;
		border-radius: var(--radius-sm);
		font-size: 0.6875rem;
		font-weight: 700;
		letter-spacing: 0.01em;
		pointer-events: none;
	}

	.hero-calendar-split__label--light {
		top: 0.65rem;
		left: 0.65rem;
		background: rgba(255, 255, 255, 0.9);
		color: #1a1a1a;
	}

	.hero-calendar-split__label--dark {
		bottom: 0.65rem;
		right: 0.65rem;
		background: rgba(16, 18, 24, 0.9);
		color: #e8eaef;
	}

	@media (min-width: 900px) {
		.m3-hero {
			grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
			gap: 3rem;
			padding: 1.75rem 0 3rem;
			min-height: 0;
			align-items: center;
		}

		.hero-phone {
			display: none;
		}

		.hero-monitor {
			display: flex;
			align-self: center;
			width: 100%;
		}

		.hero-monitor__bezel {
			padding: 0.65rem;
			border-radius: 1.35rem;
		}
	}

	/* Sections */
	.m3-section {
		padding: 3rem 0;
	}

	.reveal-on-scroll {
		opacity: 0;
		transform: translateY(18px);
		transition:
			opacity 0.65s var(--ease),
			transform 0.65s var(--ease);
		transition-delay: calc(var(--reveal-i, 0) * 60ms);
	}

	.reveal-on-scroll.is-visible {
		opacity: 1;
		transform: translateY(0);
	}

	@media (prefers-reduced-motion: reduce) {
		.reveal-on-scroll,
		[data-hero-reveal] {
			animation: none !important;
			opacity: 1 !important;
			transform: none !important;
			transition: none !important;
		}

		[data-hero-reveal].is-in {
			animation: none !important;
		}
	}

	/* Features bento */
	.features-section {
		padding-top: 2.5rem;
		border-top: 1px solid var(--border);
	}

	.features-intro {
		display: grid;
		gap: 0.75rem;
		max-width: 38rem;
		margin-bottom: 1.35rem;
	}

	.features-lead {
		max-width: 34rem;
	}

	.features-bento {
		display: flex;
		gap: 0.65rem;
		overflow-x: auto;
		overflow-y: hidden;
		scroll-snap-type: x mandatory;
		scroll-padding-inline: var(--landing-pad-x);
		-webkit-overflow-scrolling: touch;
		scrollbar-width: none;
		margin-inline: calc(var(--landing-pad-x) * -1);
		padding-inline: var(--landing-pad-x);
		padding-bottom: 0.35rem;
		mask-image: linear-gradient(to right, #000 calc(100% - 2.5rem), transparent);
	}

	@supports (mask-image: linear-gradient(to right, #000, transparent)) {
		.features-bento {
			padding-right: calc(var(--landing-pad-x) + 0.5rem);
		}
	}

	.features-bento::-webkit-scrollbar {
		display: none;
	}

	.features-bento__cell {
		position: relative;
		overflow: hidden;
		border-radius: var(--radius-lg);
		border: 1px solid var(--border);
		background: var(--surface-raised);
		flex: 0 0 min(82vw, 17.5rem);
		scroll-snap-align: start;
		min-height: 8.75rem;
		transition:
			border-color 200ms var(--ease),
			transform 200ms var(--ease);
	}

	@media (hover: hover) and (pointer: fine) {
		.features-bento__cell:hover {
			border-color: var(--border-strong);
			transform: translateY(-2px);
		}
	}

	.features-bento__photo {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		opacity: 0.55;
	}

	.features-bento__cell--featured .features-bento__photo {
		opacity: 0.45;
	}

	.features-bento__cell--featured::after {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, transparent 20%, rgba(12, 14, 20, 0.92) 85%);
		pointer-events: none;
	}

	.features-bento__cell--visual .features-bento__photo {
		opacity: 0.5;
	}

	.features-bento__cell--visual::after {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(135deg, rgba(12, 14, 20, 0.75), rgba(12, 14, 20, 0.35));
	}

	.features-bento__cell--tint {
		background: linear-gradient(145deg, rgba(66, 110, 219, 0.18), var(--surface-raised));
	}

	.features-bento__cell--featured {
		flex-basis: min(86vw, 19rem);
		min-height: 10.5rem;
	}

	.features-bento__content {
		position: relative;
		z-index: 1;
		display: grid;
		gap: 0.45rem;
		padding: 1rem 1rem 1.1rem;
		align-content: end;
		min-height: 100%;
	}

	.features-bento__cell:not(.features-bento__cell--featured):not(.features-bento__cell--visual)
		.features-bento__content {
		align-content: start;
	}

	.features-bento__icon {
		display: grid;
		place-items: center;
		width: 2.4rem;
		height: 2.4rem;
		border-radius: var(--radius-sm);
		background: var(--accent-soft);
		color: #8eb4ff;
		border: 1px solid rgba(66, 110, 219, 0.3);
	}

	.features-bento__icon .material-symbols-rounded {
		font-size: 1.2rem;
	}

	.features-bento__title {
		margin: 0;
		font-size: 1rem;
		font-weight: 800;
		letter-spacing: -0.02em;
		color: var(--text-primary);
	}

	.features-bento__text {
		margin: 0;
		font-size: 0.8125rem;
		line-height: 1.45;
		color: var(--text-secondary);
		max-width: 28rem;
		display: -webkit-box;
		-webkit-line-clamp: 3;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	@media (min-width: 640px) {
		.features-bento {
			display: grid;
			grid-template-columns: repeat(2, minmax(0, 1fr));
			gap: 0.75rem;
			overflow: visible;
			margin-inline: 0;
			padding-inline: 0;
			padding-right: 0;
			padding-bottom: 0;
			scroll-snap-type: none;
			mask-image: none;
		}

		.features-bento__cell {
			flex: unset;
			min-height: 9.5rem;
			scroll-snap-align: unset;
		}

		.features-bento__cell--featured {
			flex-basis: unset;
			grid-row: span 2;
			min-height: 16rem;
		}

		.features-bento__content {
			padding: 1.15rem 1.1rem 1.25rem;
			gap: 0.5rem;
		}

		.features-bento__title {
			font-size: 1.05rem;
		}

		.features-bento__text {
			font-size: 0.875rem;
			line-height: 1.5;
			-webkit-line-clamp: unset;
			overflow: visible;
			display: block;
		}
	}

	@media (min-width: 1024px) {
		.features-bento {
			grid-template-columns: 1.15fr 1fr 1fr;
			grid-template-rows: auto auto;
			gap: 0.85rem;
		}

		.features-bento__cell--featured {
			grid-column: 1;
			grid-row: 1 / 3;
			min-height: 18rem;
		}

		.features-bento__cell--visual {
			grid-column: 2 / 4;
			grid-row: 2;
			min-height: 11rem;
		}
	}

	/* Workflow */
	.workflow-section {
		padding-block: 3.5rem;
		border-top: 1px solid var(--border);
	}

	.workflow-intro {
		display: grid;
		gap: 0.5rem;
		margin-bottom: 2rem;
		max-width: 32rem;
	}

	.workflow-lead {
		color: var(--text-muted);
	}

	.workflow-rail {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		gap: 1rem;
	}

	.workflow-rail__step {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 1rem;
		align-items: start;
		padding: 1.25rem 1.15rem;
		border-radius: var(--radius-lg);
		border: 1px solid var(--border);
		background: var(--surface-raised);
	}

	.workflow-rail__icon {
		display: grid;
		place-items: center;
		width: 2.75rem;
		height: 2.75rem;
		border-radius: var(--radius-md);
		background: var(--accent-soft);
		color: #8eb4ff;
		font-size: 1.35rem;
	}

	.workflow-rail__copy {
		display: grid;
		gap: 0.35rem;
		min-width: 0;
	}

	@media (min-width: 900px) {
		.workflow-rail {
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 1rem;
			align-items: stretch;
		}

		.workflow-rail__step {
			grid-template-columns: 1fr;
			padding: 1.5rem 1.25rem;
		}
	}

	/* CTA split */
	.m3-cta-split {
		display: grid;
		gap: 1.5rem;
		padding: 2.5rem 1.25rem;
		margin-bottom: 1rem;
		border-radius: var(--radius-lg);
		border: 1px solid rgba(66, 110, 219, 0.25);
		background:
			radial-gradient(ellipse 80% 80% at 100% 0%, rgba(66, 110, 219, 0.15), transparent 55%),
			var(--surface-raised);
	}

	.cta-split__copy {
		display: grid;
		gap: 0.75rem;
	}

	.cta-premium {
		color: #8eb4ff;
	}

	.cta-split__panel {
		display: grid;
		gap: 1.25rem;
		padding: 1.25rem;
		border-radius: var(--radius-md);
		background: rgba(0, 0, 0, 0.22);
		border: 1px solid var(--border);
	}

	.cta-checklist {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		gap: 0.65rem;
	}

	.cta-checklist li {
		display: flex;
		align-items: center;
		gap: 0.55rem;
		font-size: 0.9375rem;
		font-weight: 600;
		color: var(--text-secondary);
	}

	.cta-checklist .material-symbols-rounded {
		font-size: 1.15rem;
		color: #8eb4ff;
		flex-shrink: 0;
	}

	.cta-btn {
		align-self: flex-start;
	}

	@media (min-width: 768px) {
		.m3-cta-split {
			grid-template-columns: 1fr 1fr;
			align-items: center;
			padding: 2.75rem 2rem;
			gap: 2rem;
		}
	}

	/* Footer */
	.m3-footer {
		width: 100%;
		padding: 2rem 0 1.5rem;
		color: var(--text-muted);
		font-size: 0.875rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 1.15rem;
		border-top: 1px solid var(--border);
	}

	.m3-footer-brand,
	.m3-footer-support,
	.m3-footer-nav {
		width: 100%;
		margin: 0;
	}

	.m3-footer-links {
		display: contents;
	}

	.m3-footer-brand {
		display: grid;
		gap: 0.15rem;
	}

	.m3-footer-copy,
	.m3-footer-tagline {
		margin: 0;
	}

	.m3-footer-copy {
		color: var(--text-primary);
		font-weight: 700;
	}

	.m3-footer-support {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.25rem;
	}

	.m3-footer-nav {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.65rem;
	}

	.m3-footer-link {
		color: var(--accent);
		font-weight: 700;
		transition: color 200ms var(--ease);
	}

	.m3-footer-link:hover {
		color: var(--accent-hover);
	}

	@media (min-width: 640px) {
		.m3-footer-support {
			flex-direction: row;
			flex-wrap: wrap;
			justify-content: center;
			gap: 0.35rem;
		}

		.m3-footer-nav {
			flex-direction: row;
			flex-wrap: wrap;
			justify-content: center;
			gap: 0.35rem 1.25rem;
		}
	}

	@media (min-width: 1024px) {
		.m3-footer {
			flex-direction: row;
			align-items: center;
			justify-content: space-between;
			text-align: left;
			padding-bottom: 2rem;
		}

		.m3-footer-brand {
			text-align: left;
		}

		.m3-footer-links {
			display: flex;
			flex-direction: column;
			align-items: flex-end;
			gap: 0.5rem;
		}

		.m3-footer-support {
			justify-content: flex-end;
		}

		.m3-footer-nav {
			justify-content: flex-end;
		}
	}
</style>
