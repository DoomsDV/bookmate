---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
const siteOrigin = String(Astro.site ?? Astro.url.origin).replace(/\/+$/, '');
const pageTitle = 'Hasel — Software de citas, agenda y reservas online para tu negocio';
const metaDescription =
	'Organizá citas, equipo y clientes en un solo panel. Tus clientes reservan 24/7 con tu enlace personalizado. Probá Premium gratis 14 días.';

const showcaseFeatures = [
	{
		id: 'voice-ai',
		eyebrow: 'Recepción inteligente',
		title: 'Cargá citas hablando, con IA',
		badge: 'Incluido en Premium',
		blocks: [
			{
				icon: 'smartphone',
				title: 'Uso en movimiento',
				text: 'Pensado para que cargues turnos al instante mientras estás ocupado o atendiendo en el mostrador.',
			},
			{
				icon: 'psychology',
				title: 'Lenguaje natural',
				text: 'Olvidate de los formularios. Dictá los datos y la IA arma la cita directamente en tu calendario.',
			},
		],
		visual: 'voice',
	},
	{
		id: 'deposits',
		eyebrow: 'Cobro de señas',
		title: 'Pedí seña y asegurá tu tiempo',
		badge: 'Incluido en Premium',
		blocks: [
			{
				icon: 'account_balance',
				title: 'Transferencia directa',
				text: 'El cliente te transfiere la seña a tu cuenta. El dinero llega sin intermediarios.',
			},
			{
				icon: 'verified',
				title: 'Validación y políticas',
				text: 'Recibí el comprobante, validalo y el turno queda confirmado. Protegé tus ingresos con reglas claras que el cliente acepta al reservar.',
			},
		],
		visual: 'deposit',
	},
	{
		id: 'history',
		eyebrow: 'Historial por visita',
		title: 'El historial del cliente, en cada cita',
		badge: 'Incluido en Premium',
		blocks: [
			{
				icon: 'note_alt',
				title: 'Notas y archivos',
				text: 'Al completar un turno, guardá el contexto de la visita: notas, fotos o documentos en un solo lugar.',
			},
			{
				icon: 'person_search',
				title: 'Atención personalizada',
				text: 'Llevá un registro centralizado de la evolución de tus clientes para brindar una atención mejor en cada nueva visita.',
			},
		],
		visual: 'history',
	},
] as const;

const steps = [
	{
		step: '01',
		title: 'Configurá en minutos',
		text: 'Definí tus servicios, horarios y profesionales en un panel intuitivo. Tu negocio listo para operar al instante.',
		icon: 'storefront',
	},
	{
		step: '02',
		title: 'Compartí tu enlace',
		text: 'Pegá tu enlace en WhatsApp, Instagram o tu bio. Tus clientes ven tu disponibilidad real y agendan solos 24/7.',
		icon: 'link',
	},
	{
		step: '03',
		title: 'Todo bajo control',
		text: 'El calendario se actualiza solo. Recibí alertas al instante y dejá que Hasel envíe los recordatorios automáticos por vos.',
		icon: 'calendar_month',
	},
];

const jsonLd = [
	{
		'@context': 'https://schema.org',
		'@type': 'WebSite',
		name: 'Hasel',
		url: siteOrigin,
		description: metaDescription,
		inLanguage: 'es',
	},
	{
		'@context': 'https://schema.org',
		'@type': 'SoftwareApplication',
		name: 'Hasel',
		applicationCategory: 'BusinessApplication',
		operatingSystem: 'Web',
		url: siteOrigin,
		description: metaDescription,
		offers: {
			'@type': 'Offer',
			price: '0',
			priceCurrency: 'PYG',
			description: 'Prueba Premium 14 días sin tarjeta',
		},
		featureList: [
			'Citas por voz con IA',
			'Cobro de señas por transferencia',
			'Historial de visitas con notas y archivos',
			'Reservas públicas por enlace personalizado',
			'Prueba Premium 14 días',
			...steps.map((step) => step.title),
		].join(', '),
	},
];

---

<Layout
	pageTitle={pageTitle}
	metaDescription={metaDescription}
	canonicalPath="/"
	ogImage="/icon-512x512.png"
	jsonLd={jsonLd}
>
	<main class="landing-root">
		<div class="landing-orb landing-orb-a" aria-hidden="true"></div>
		<div class="landing-orb landing-orb-b" aria-hidden="true"></div>
		<div class="landing-orb landing-orb-c" aria-hidden="true"></div>

		<header class="landing-topbar" data-landing-topbar>
			<div class="landing-topbar-inner">
				<a href="/" class="brand no-underline" aria-label="Hasel — Inicio">
					<img
						src="/icon-512x512.png"
						alt=""
						class="brand-logo"
						width="32"
						height="32"
						loading="eager"
						decoding="async"
					/>
					<span class="brand-text">Hasel</span>
				</a>

				<nav class="landing-topbar-links" aria-label="Secciones">
					<a href="#funciones" class="landing-topbar-link no-underline">Funciones</a>
					<a href="#precios" class="landing-topbar-link no-underline">Precios</a>
					<a href="#soporte" class="landing-topbar-link no-underline">Soporte</a>
				</nav>

				<div class="nav-actions">
					<a href="/auth/login" class="m3-btn m3-btn-ghost m3-btn-nav no-underline">Iniciar sesión</a>
					<a href="/auth/register" class="m3-btn m3-btn-primary m3-btn-nav no-underline">Empezar gratis</a>
				</div>
			</div>
		</header>

		<div class="landing-shell">
			<section class="m3-hero" aria-labelledby="hero-heading">
				<div class="m3-hero-content">
					<p class="m3-chip m3-chip-primary">
						<span class="material-symbols-rounded" aria-hidden="true">auto_awesome</span>
						<span>14 días Premium de prueba</span>
					</p>
					<h1 id="hero-heading" class="m3-display">
						Tu calendario, equipo y reservas públicas en una sola plataforma
					</h1>
					<p class="m3-body-large">
						Tus clientes reservan 24/7; vos gestionás el día desde un solo lugar.
					</p>

					<div class="hero-actions">
						<a href="/auth/register" class="m3-btn m3-btn-primary m3-btn-large no-underline">
							Crear mi espacio gratis
							<span class="material-symbols-rounded" aria-hidden="true">arrow_forward</span>
						</a>
						<a href="#funciones" class="m3-btn m3-btn-tonal m3-btn-large no-underline">Ver funciones</a>
					</div>
				</div>

				<div class="m3-hero-visuals" aria-hidden="true">
					<div class="visual-story-shell">
						<div class="visual-card visual-card-main">
							<div class="visual-header">
								<div class="visual-avatar">
									<span class="material-symbols-rounded">event</span>
								</div>
								<div class="visual-info">
									<strong>Reserva recibida</strong>
									<span>Desde tu enlace público</span>
								</div>
								<div class="visual-badge visual-badge-success">Pendiente</div>
							</div>
						</div>

						<div class="visual-metrics-grid">
							<div class="visual-card visual-card-3">
								<div class="visual-icon-box visual-icon-ai">
									<span class="material-symbols-rounded">groups</span>
								</div>
								<div class="visual-info">
									<strong>Equipo y sucursales</strong>
									<span>Sin cupo artificial de asientos</span>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<section id="funciones" class="m3-section" aria-labelledby="funciones-heading">
				<div class="section-header center reveal-on-scroll">
					<h2 id="funciones-heading" class="m3-headline">Herramientas avanzadas para escalar tu negocio</h2>
					<p class="m3-body section-lead">
						Recepción por voz con IA, cobro de señas e historial de clientes. Todo el poder de Hasel
						incluido en tu prueba de 14 días.
					</p>
				</div>

				<div class="feature-showcase">
					{
						showcaseFeatures.map((feature) => (
							<article class="feature-stack reveal-on-scroll">
								<header class="feature-stack-head">
									<div class="feature-stack-labels">
										<p class="feature-eyebrow">{feature.eyebrow}</p>
										<span class="feature-badge">{feature.badge}</span>
									</div>
									<h3 class="feature-title">{feature.title}</h3>
								</header>

								<div class="feature-visual" aria-hidden="true">
									<div class="feature-visual-glow"></div>
									{feature.visual === 'voice' ? (
										<div class="ui-panel ui-panel--voice">
											<div class="ui-voice-header">
												<span class="ui-voice-mic material-symbols-rounded">mic</span>
												<strong>Cita rápida por voz</strong>
											</div>
											<p class="ui-voice-quote">
												“Agendá a Ana Ríos mañana a las 10:30 para consulta general con María”
											</p>
											<div class="ui-voice-result">
												<span class="ui-pill-soft">Lista para confirmar</span>
												<small>Ana Ríos · Consulta · Mañana 10:30 · María</small>
											</div>
										</div>
									) : feature.visual === 'deposit' ? (
										<div class="ui-panel ui-panel--deposit">
											<div class="ui-deposit-header">
												<strong>Seña recibida</strong>
												<span>Gs. 50.000</span>
											</div>
											<div class="ui-deposit-code">
												<em>Transferencia a tu cuenta</em>
												<strong>Comprobante listo</strong>
											</div>
											<div class="ui-deposit-row">
												<span>Validación del pago</span>
												<em>En revisión</em>
											</div>
											<div class="ui-booking-cta">Confirmar turno</div>
										</div>
									) : (
										<div class="ui-panel ui-panel--history">
											<div class="ui-history-header">
												<strong>Ana Ríos</strong>
												<span>Historial de visitas</span>
											</div>
											<div class="ui-history-card">
												<small>12 jun · Completado</small>
												<strong>Consulta de control</strong>
												<p>Nota: Respondió bien al plan. Próximo control en 30 días.</p>
												<span class="ui-file-chip">foto-control.jpg</span>
											</div>
											<div class="ui-history-card ui-history-card--muted">
												<small>15 may · Completado</small>
												<strong>Primera consulta</strong>
												<p>Archivo: consentimiento.pdf</p>
											</div>
										</div>
									)}
								</div>

								<div class="feature-microblocks">
									{feature.blocks.map((block) => (
										<div class="feature-microblock">
											<span class="feature-microblock-icon material-symbols-rounded" aria-hidden="true">
												{block.icon}
											</span>
											<div class="feature-microblock-copy">
												<strong>{block.title}</strong>
												<p>{block.text}</p>
											</div>
										</div>
									))}
								</div>
							</article>
						))
					}
				</div>
			</section>

			<section id="como-funciona" class="m3-section m3-section-tonal" aria-labelledby="como-funciona-heading">
				<div class="workflow-intro reveal-on-scroll">
					<p class="m3-chip m3-chip-secondary">
						<span class="material-symbols-rounded" aria-hidden="true">bolt</span>
						<span>Listo para usar</span>
					</p>
					<h2 id="como-funciona-heading" class="m3-headline">Cómo funciona Hasel</h2>
				</div>

				<ol class="workflow-steps-grid">
					{
						steps.map((item, index) => (
							<li class="workflow-step-card reveal-on-scroll">
								<span class="workflow-step-icon material-symbols-rounded" aria-hidden="true">
									{item.icon}
								</span>
								<span class="workflow-step-num">{item.step}</span>
								<h3 class="m3-title">{item.title}</h3>
								<p class="m3-body">{item.text}</p>
								{index < steps.length - 1 ? (
									<span class="workflow-step-arrow material-symbols-rounded" aria-hidden="true">
										arrow_forward
									</span>
								) : null}
							</li>
						))
					}
				</ol>
			</section>

			<section id="precios" class="m3-cta-card" aria-labelledby="cta-heading">
				<div class="cta-content">
					<h2 id="cta-heading" class="m3-headline">Empezá con 14 días de Premium</h2>
					<p class="m3-body-large">
						Creá tu espacio, configurá horarios y compartí tu página pública. Tus clientes
						reservan; vos controlás todo desde el calendario.
					</p>
					<a href="/auth/register" class="m3-btn m3-btn-primary m3-btn-large no-underline cta-btn">
						Crear mi espacio gratis
						<span class="material-symbols-rounded" aria-hidden="true">arrow_forward</span>
					</a>
					<p class="cta-reassure">
						<span class="material-symbols-rounded" aria-hidden="true">check_circle</span>
						Sin tarjeta de crédito requerida
					</p>
				</div>
			</section>

			<footer id="soporte" class="m3-footer">
				<p>© {new Date().getFullYear()} Hasel — Software de citas y reservas online.</p>
				<p class="m3-footer-support">
					¿Necesitás ayuda? Escribinos a
					<a href="mailto:support@hasel.app" class="m3-footer-link no-underline">support@hasel.app</a>
				</p>
				<nav class="m3-footer-nav" aria-label="Enlaces legales">
					<a href="/politicas-y-privacidad" class="m3-footer-link no-underline">
						Políticas y privacidad
					</a>
					<a href="/politicas-de-cancelacion-y-reembolso" class="m3-footer-link no-underline">
						Cancelación y reembolso
					</a>
				</nav>
			</footer>
		</div>
	</main>
</Layout>

<script is:inline>
	(() => {
		if (typeof window === 'undefined') return;

		const nav = document.querySelector('[data-landing-topbar]');
		const getOffset = () => {
			const navHeight = nav instanceof HTMLElement ? nav.getBoundingClientRect().height : 0;
			return navHeight + 12;
		};

		const syncTopbar = () => {
			if (!(nav instanceof HTMLElement)) return;
			nav.classList.toggle('is-scrolled', window.scrollY > 12);
		};
		syncTopbar();
		window.addEventListener('scroll', syncTopbar, { passive: true });

		const scrollToHash = (hash, behavior = 'smooth') => {
			if (!hash || !hash.startsWith('#')) return;
			const id = decodeURIComponent(hash.slice(1));
			if (!id) return;
			const target = document.getElementById(id);
			if (!target) return;

			const top = target.getBoundingClientRect().top + window.scrollY - getOffset();
			window.scrollTo({ top: Math.max(0, top), behavior });
		};

		document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
			anchor.addEventListener('click', (event) => {
				const href = anchor.getAttribute('href');
				if (!href) return;
				event.preventDefault();
				history.pushState(null, '', href);
				scrollToHash(href, 'smooth');
			});
		});

		if (window.location.hash) {
			requestAnimationFrame(() => scrollToHash(window.location.hash, 'auto'));
		}

		const revealNodes = document.querySelectorAll('.reveal-on-scroll');
		if (revealNodes.length && 'IntersectionObserver' in window) {
			const revealObserver = new IntersectionObserver(
				(entries) => {
					for (const entry of entries) {
						if (!entry.isIntersecting) continue;
						entry.target.classList.add('is-visible');
						revealObserver.unobserve(entry.target);
					}
				},
				{ rootMargin: '0px 0px -12% 0px', threshold: 0.18 }
			);
			revealNodes.forEach((node) => revealObserver.observe(node));
		} else {
			revealNodes.forEach((node) => node.classList.add('is-visible'));
		}
	})();
</script>

<style>
	/* PREMIUM DARK — single palette, no light/dark duality */
	:root {
		--m3-sys-light-primary: #3a6fd7;
		--m3-sys-light-on-primary: #ffffff;
		--m3-sys-light-primary-container: rgba(58, 111, 215, 0.2);
		--m3-sys-light-on-primary-container: #bfd6ff;

		--m3-sys-light-secondary: #6b8ecf;
		--m3-sys-light-secondary-container: rgba(148, 163, 184, 0.12);
		--m3-sys-light-on-secondary-container: #dbe7ff;

		--m3-sys-light-surface: #09090b;
		--m3-sys-light-surface-container: rgba(255, 255, 255, 0.04);
		--m3-sys-light-on-surface: #ffffff;
		--m3-sys-light-on-surface-variant: #a1a1aa;

		--m3-sys-light-outline: #3f3f46;
		--m3-sys-light-outline-variant: rgba(255, 255, 255, 0.1);
	}

	.landing-root {
		position: relative;
		overflow: hidden;
		min-height: 100vh;
		background-color: var(--m3-sys-light-surface);
		color: var(--m3-sys-light-on-surface);
		font-family: 'Inter', system-ui, sans-serif;
	}

	html {
		scroll-padding-top: 5.5rem;
	}

	.landing-root [id] {
		scroll-margin-top: 5.5rem;
	}

	.landing-orb {
		position: absolute;
		border-radius: 9999px;
		filter: blur(92px);
		pointer-events: none;
		z-index: 0;
		animation: landing-orb-drift 16s ease-in-out infinite;
		will-change: transform, opacity;
	}

	.landing-orb-a {
		top: -15%;
		left: -10%;
		width: 55vw;
		height: 55vw;
		background: radial-gradient(circle, rgba(59, 130, 246, 0.28), transparent 65%);
		animation-duration: 18s;
	}

	.landing-orb-b {
		bottom: -15%;
		right: -10%;
		width: 65vw;
		height: 65vw;
		background: radial-gradient(circle, rgba(14, 165, 233, 0.22), transparent 65%);
		animation-duration: 22s;
		animation-direction: reverse;
	}

	.landing-orb-c {
		top: 38%;
		left: 18%;
		width: 35vw;
		height: 35vw;
		background: radial-gradient(circle, rgba(99, 102, 241, 0.18), transparent 65%);
		animation-duration: 19s;
		animation-delay: -4s;
	}

	.landing-shell {
		position: relative;
		z-index: 1;
		width: min(1200px, 100%);
		margin: 0 auto;
		padding: 1rem;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.landing-topbar {
		position: sticky;
		top: 0;
		z-index: 50;
		width: 100%;
		padding: 0.85rem 0;
		background: transparent;
		border-bottom: 1px solid transparent;
		transition:
			background-color 0.25s ease,
			border-color 0.25s ease,
			backdrop-filter 0.25s ease,
			box-shadow 0.25s ease;
	}

	.landing-topbar.is-scrolled {
		background: rgba(9, 9, 11, 0.72);
		border-bottom-color: rgba(255, 255, 255, 0.1);
		backdrop-filter: blur(16px);
		-webkit-backdrop-filter: blur(16px);
		box-shadow: 0 10px 28px rgba(0, 0, 0, 0.22);
	}

	.landing-topbar-inner {
		width: min(1200px, 100%);
		margin: 0 auto;
		padding: 0 1rem;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
	}

	.landing-topbar-links {
		display: none;
		justify-content: center;
		align-items: center;
		gap: 1.5rem;
	}

	.landing-topbar-link {
		color: #a1a1aa;
		font-size: 0.92rem;
		font-weight: 600;
		transition: color 0.2s ease;
	}

	.landing-topbar-link:hover {
		color: #ffffff;
	}

	.brand {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		color: var(--m3-sys-light-on-surface);
	}

	.brand-logo {
		height: 2rem;
		width: 2rem;
		flex-shrink: 0;
		object-fit: contain;
		border-radius: 999px;
	}

	.brand-text {
		font-weight: 700;
		font-size: 1.25rem;
		letter-spacing: -0.02em;
	}

	.nav-actions {
		display: flex;
		align-items: center;
		gap: 0.35rem;
		flex-shrink: 0;
		justify-self: end;
	}

	@media (min-width: 768px) {
		.landing-shell {
			padding: 1.5rem;
			gap: 1.5rem;
		}

		.landing-topbar-inner {
			display: grid;
			grid-template-columns: auto 1fr auto;
			padding: 0 1.5rem;
			gap: 1rem;
		}

		.landing-topbar-links {
			display: flex;
		}
	}

	.m3-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
		border-radius: 999px;
		font-weight: 500;
		font-size: 0.95rem;
		padding: 0.625rem 1.5rem;
		transition: all 0.2s cubic-bezier(0.2, 0, 0, 1);
		cursor: pointer;
		border: 1px solid transparent;
		text-decoration: none;
	}

	.m3-btn:hover {
		transform: translateY(-1px);
		box-shadow: 0 4px 10px rgba(2, 6, 23, 0.22);
	}

	.m3-btn:active {
		transform: translateY(0);
	}

	.m3-btn-large {
		padding: 0.875rem 2rem;
		font-size: 1.05rem;
	}

	.m3-btn-ghost {
		background: transparent;
		color: #d4d4d8;
		border-color: transparent;
		box-shadow: none;
	}

	.m3-btn-ghost:hover {
		color: #ffffff;
		background: rgba(255, 255, 255, 0.06);
		box-shadow: none;
		transform: none;
	}

	.m3-btn-primary {
		position: relative;
		isolation: isolate;
		background: linear-gradient(180deg, #4a7fdc 0%, #3a6fd7 55%, #335fa9 100%);
		color: var(--m3-sys-light-on-primary);
		border-color: rgba(147, 197, 253, 0.35);
		box-shadow:
			0 0 0 1px rgba(147, 197, 253, 0.12),
			0 10px 28px rgba(58, 111, 215, 0.28);
	}

	.m3-btn-primary::before {
		content: '';
		position: absolute;
		inset: -1px;
		z-index: -1;
		border-radius: inherit;
		background: linear-gradient(
			120deg,
			rgba(191, 214, 255, 0.55),
			rgba(58, 111, 215, 0.15) 40%,
			rgba(125, 211, 252, 0.45)
		);
		opacity: 0.55;
		transition: opacity 0.25s ease, filter 0.25s ease;
		filter: blur(0.5px);
	}

	.m3-btn-primary:hover {
		background: linear-gradient(180deg, #5689e2 0%, #4478d9 55%, #3a6fd7 100%);
		box-shadow:
			0 0 0 1px rgba(191, 214, 255, 0.28),
			0 14px 36px rgba(58, 111, 215, 0.42),
			0 0 28px rgba(125, 211, 252, 0.22);
	}

	.m3-btn-primary:hover::before {
		opacity: 1;
		filter: blur(1px);
	}

	.m3-btn-tonal {
		background-color: rgba(30, 41, 59, 0.42);
		color: var(--m3-sys-light-on-secondary-container);
		border-color: rgba(148, 163, 184, 0.32);
	}

	.m3-btn-tonal:hover {
		background-color: rgba(51, 65, 85, 0.56);
		border-color: rgba(203, 213, 225, 0.36);
	}

	.m3-btn-nav {
		padding: 0.55rem 1.1rem;
		font-weight: 600;
		letter-spacing: 0.01em;
		font-size: 0.9rem;
	}

	@media (max-width: 480px) {
		.nav-actions .m3-btn-ghost {
			padding-inline: 0.7rem;
			font-size: 0.84rem;
		}

		.nav-actions .m3-btn-primary {
			padding-inline: 0.85rem;
			font-size: 0.84rem;
		}
	}

	.m3-chip {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		border-radius: 999px;
		font-size: 0.875rem;
		font-weight: 600;
		background: var(--m3-sys-light-surface-container);
		color: var(--m3-sys-light-on-surface);
		border: 1px solid var(--m3-sys-light-outline-variant);
		margin: 0;
	}

	.m3-chip-primary {
		background: var(--m3-sys-light-primary-container);
		color: var(--m3-sys-light-on-primary-container);
		border-color: transparent;
	}

	.m3-chip-secondary {
		background: var(--m3-sys-light-secondary-container);
		color: var(--m3-sys-light-on-secondary-container);
		border-color: transparent;
	}

	.m3-chip .material-symbols-rounded {
		font-size: 1.125rem;
	}

	.m3-hero {
		display: grid;
		gap: 3rem;
		padding: 1rem 1rem 2rem;
		align-items: center;
	}

	.m3-hero-content {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 1.5rem;
	}

	.m3-display {
		font-size: clamp(2.5rem, 6vw, 4.5rem);
		font-weight: 700;
		line-height: 1.05;
		letter-spacing: -0.03em;
		color: var(--m3-sys-light-on-surface);
		margin: 0;
	}

	.m3-body-large {
		font-size: clamp(1rem, 2vw, 1.25rem);
		line-height: 1.6;
		color: var(--m3-sys-light-on-surface-variant);
		max-width: 54ch;
		margin: 0;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
		margin-top: 0.25rem;
	}

	.m3-hero-visuals {
		position: relative;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		width: 100%;
	}

	.visual-story-shell {
		display: contents;
	}

	.visual-card {
		background: rgba(255, 255, 255, 0.04);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 1.25rem;
		padding: 0.875rem 1rem;
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.08);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		display: flex;
		align-items: center;
		gap: 0.875rem;
		width: 100%;
		animation: float 7s ease-in-out infinite;
	}

	.visual-card-main {
		animation-delay: 0s;
	}

	.visual-metrics-grid {
		display: contents;
	}

	.visual-card-2 {
		animation-delay: -2.4s;
	}

	.visual-card-3 {
		animation-delay: -4.2s;
	}

	@media (min-width: 1024px) {
		.m3-hero-visuals {
			position: relative;
			height: 360px;
			display: block;
			width: auto;
		}

		.visual-card {
			position: absolute;
			width: auto;
			padding: 1.25rem;
			border-radius: 1.5rem;
		}

		.visual-card-main {
			top: 5%;
			left: 0;
			z-index: 2;
		}

		.visual-card-2 {
			bottom: 5%;
			left: 0;
			z-index: 1;
		}

		.visual-card-3 {
			top: 38%;
			right: 0;
			z-index: 3;
		}

		.m3-hero {
			grid-template-columns: 1fr 1fr;
			padding: 2rem 2rem 3rem;
		}
	}

	@keyframes float {
		0%,
		100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-9px);
		}
	}

	@keyframes landing-orb-drift {
		0%,
		100% {
			transform: translate3d(0, 0, 0) scale(1);
			opacity: 0.82;
		}
		25% {
			transform: translate3d(10%, -2%, 0) scale(1.05);
			opacity: 0.96;
		}
		50% {
			transform: translate3d(22%, 1%, 0) scale(1.1);
			opacity: 1;
		}
		75% {
			transform: translate3d(10%, -2%, 0) scale(1.05);
			opacity: 0.94;
		}
	}

	.visual-icon-ai {
		background: rgba(148, 163, 184, 0.22);
		color: #dbeafe;
	}

	.visual-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		justify-content: space-between;
		width: 100%;
	}

	.visual-avatar,
	.visual-icon-box {
		width: 3rem;
		height: 3rem;
		border-radius: 50%;
		display: grid;
		place-items: center;
		flex-shrink: 0;
	}

	.visual-avatar {
		background: rgba(226, 232, 240, 0.9);
		color: #1e293b;
	}

	.visual-icon-box {
		background: rgba(191, 219, 254, 0.9);
		color: #1e3a8a;
	}

	.visual-info {
		display: flex;
		flex-direction: column;
		min-width: 0;
	}

	.visual-info strong {
		font-size: 0.95rem;
		color: var(--m3-sys-light-on-surface);
	}

	.visual-info span {
		font-size: 0.85rem;
		color: var(--m3-sys-light-on-surface-variant);
	}

	.visual-badge {
		padding: 0.25rem 0.65rem;
		border-radius: 999px;
		font-size: 0.74rem;
		font-weight: 700;
		flex-shrink: 0;
	}

	.visual-badge-success {
		background: rgba(34, 197, 94, 0.24);
		border: 1px solid rgba(74, 222, 128, 0.3);
		color: #bbf7d0;
	}

	.m3-section {
		padding: 2rem 1rem;
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	@media (min-width: 768px) {
		.m3-section {
			padding: 4rem 1.5rem;
			gap: 3rem;
		}
	}

	.m3-section-tonal {
		background: rgba(255, 255, 255, 0.03);
		border: 1px solid rgba(255, 255, 255, 0.08);
		border-radius: 2rem;
		backdrop-filter: blur(8px);
		-webkit-backdrop-filter: blur(8px);
	}

	.section-header.center {
		text-align: center;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.75rem;
	}

	.section-lead {
		max-width: 62ch;
	}

	@media (max-width: 899px) {
		.section-header.center .section-lead {
			display: none;
		}

		#funciones.m3-section {
			padding: 1.75rem 0.85rem;
			gap: 1.35rem;
		}

		.feature-showcase {
			gap: 2.75rem;
		}
	}

	.m3-headline {
		font-size: clamp(2rem, 4vw, 3rem);
		font-weight: 700;
		letter-spacing: -0.02em;
		margin: 0;
		color: var(--m3-sys-light-on-surface);
	}

	.m3-body {
		font-size: 1rem;
		line-height: 1.5;
		color: var(--m3-sys-light-on-surface-variant);
		margin: 0;
	}

	.reveal-on-scroll {
		opacity: 0.28;
		transform: translateY(18px);
		transition:
			opacity 0.7s cubic-bezier(0.2, 0, 0, 1),
			transform 0.7s cubic-bezier(0.2, 0, 0, 1),
			color 0.7s ease;
	}

	.reveal-on-scroll.is-visible {
		opacity: 1;
		transform: translateY(0);
	}

	@media (prefers-reduced-motion: reduce) {
		.reveal-on-scroll {
			opacity: 1;
			transform: none;
			transition: none;
		}

		.landing-orb {
			animation: none;
		}
	}

	.feature-showcase {
		display: grid;
		gap: 3.25rem;
	}

	.feature-stack {
		display: grid;
		gap: 1.15rem;
		width: min(100%, 36rem);
		margin: 0 auto;
	}

	.feature-stack-head {
		display: grid;
		gap: 0.55rem;
	}

	.feature-stack-labels {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.55rem 0.7rem;
	}

	.feature-eyebrow {
		margin: 0;
		color: #bfd6ff;
		font-size: 0.8rem;
		font-weight: 700;
		letter-spacing: 0.06em;
		text-transform: uppercase;
	}

	.feature-badge {
		display: inline-flex;
		align-items: center;
		padding: 0.28rem 0.65rem;
		border-radius: 999px;
		background: rgba(58, 111, 215, 0.22);
		border: 1px solid rgba(147, 197, 253, 0.28);
		color: #bfd6ff;
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.01em;
		white-space: nowrap;
	}

	.feature-title {
		margin: 0;
		color: #ffffff;
		font-size: clamp(1.45rem, 3.2vw, 2.15rem);
		font-weight: 720;
		letter-spacing: -0.03em;
		line-height: 1.15;
	}

	.feature-visual {
		position: relative;
		min-width: 0;
		isolation: isolate;
	}

	.feature-microblocks {
		display: grid;
		gap: 1.1rem;
		padding-top: 0.15rem;
	}

	.feature-microblock {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.85rem;
		align-items: start;
	}

	.feature-microblock-icon {
		display: grid;
		place-items: center;
		width: 2.35rem;
		height: 2.35rem;
		border-radius: 0.8rem;
		background: rgba(58, 111, 215, 0.16);
		border: 1px solid rgba(255, 255, 255, 0.1);
		color: #bfd6ff;
		font-size: 1.2rem;
	}

	.feature-microblock-copy {
		display: grid;
		gap: 0.25rem;
		padding-top: 0.1rem;
	}

	.feature-microblock-copy strong {
		color: #f4f4f5;
		font-size: 1rem;
		font-weight: 700;
		letter-spacing: -0.01em;
	}

	.feature-microblock-copy p {
		margin: 0;
		color: var(--m3-sys-light-on-surface-variant);
		font-size: 0.92rem;
		line-height: 1.55;
	}

	.feature-visual-glow {
		position: absolute;
		z-index: 0;
		inset: 8% 4% 4%;
		border-radius: 50%;
		background: radial-gradient(
			circle at 50% 45%,
			rgba(58, 111, 215, 0.32) 0%,
			rgba(99, 102, 241, 0.14) 42%,
			transparent 72%
		);
		filter: blur(18px);
		pointer-events: none;
	}

	.ui-panel {
		position: relative;
		z-index: 1;
		border: 1px solid rgba(255, 255, 255, 0.12);
		border-radius: 1.35rem;
		background: linear-gradient(160deg, rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.025));
		box-shadow:
			0 0 0 1px rgba(255, 255, 255, 0.04),
			0 28px 60px rgba(0, 0, 0, 0.38),
			inset 0 1px 0 rgba(255, 255, 255, 0.1);
		padding: 1.1rem;
		overflow: hidden;
	}

	.ui-panel--voice,
	.ui-panel--deposit,
	.ui-panel--history {
		display: grid;
		gap: 0.9rem;
	}

	.ui-voice-header {
		display: flex;
		align-items: center;
		gap: 0.65rem;
	}

	.ui-voice-mic {
		display: grid;
		place-items: center;
		width: 2.5rem;
		height: 2.5rem;
		border-radius: 999px;
		background: rgba(58, 111, 215, 0.28);
		color: #dbeafe;
		font-size: 1.25rem;
		box-shadow: 0 0 20px rgba(58, 111, 215, 0.35);
	}

	.ui-voice-header strong {
		color: #f4f4f5;
		font-size: 0.95rem;
	}

	.ui-voice-quote {
		margin: 0;
		padding: 0.9rem 1rem;
		border-radius: 1rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		background: rgba(0, 0, 0, 0.28);
		color: #d4d4d8;
		font-size: 0.92rem;
		line-height: 1.5;
		font-style: italic;
	}

	.ui-voice-result {
		display: grid;
		gap: 0.35rem;
	}

	.ui-pill-soft {
		display: inline-flex;
		width: fit-content;
		padding: 0.28rem 0.65rem;
		border-radius: 999px;
		background: rgba(34, 197, 94, 0.18);
		border: 1px solid rgba(74, 222, 128, 0.28);
		color: #bbf7d0;
		font-size: 0.72rem;
		font-weight: 700;
	}

	.ui-voice-result small {
		color: #a1a1aa;
		font-size: 0.82rem;
	}

	.ui-deposit-header {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: 0.75rem;
	}

	.ui-deposit-header strong {
		color: #f4f4f5;
		font-size: 0.95rem;
	}

	.ui-deposit-header span {
		color: #93c5fd;
		font-size: 0.95rem;
		font-weight: 700;
	}

	.ui-deposit-code {
		display: grid;
		gap: 0.25rem;
		padding: 0.85rem 1rem;
		border-radius: 1rem;
		border: 1px dashed rgba(147, 197, 253, 0.35);
		background: rgba(58, 111, 215, 0.12);
	}

	.ui-deposit-code em {
		font-style: normal;
		color: #93c5fd;
		font-size: 0.72rem;
		font-weight: 650;
		letter-spacing: 0.04em;
		text-transform: uppercase;
	}

	.ui-deposit-code strong {
		color: #ffffff;
		font-size: 1.15rem;
		letter-spacing: 0.06em;
	}

	.ui-deposit-row {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
		color: #c4c4cc;
		font-size: 0.88rem;
	}

	.ui-deposit-row em {
		font-style: normal;
		color: #fde68a;
		font-size: 0.78rem;
		font-weight: 700;
	}

	.ui-booking-cta {
		display: inline-flex;
		justify-content: center;
		padding: 0.7rem 1rem;
		border-radius: 999px;
		background: #3a6fd7;
		color: #ffffff;
		font-size: 0.9rem;
		font-weight: 650;
	}

	.ui-history-header {
		display: grid;
		gap: 0.15rem;
	}

	.ui-history-header strong {
		color: #f4f4f5;
		font-size: 1rem;
	}

	.ui-history-header span {
		color: #a1a1aa;
		font-size: 0.8rem;
	}

	.ui-history-card {
		display: grid;
		gap: 0.3rem;
		padding: 0.8rem 0.9rem;
		border-radius: 0.95rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		background: rgba(0, 0, 0, 0.25);
	}

	.ui-history-card--muted {
		opacity: 0.78;
	}

	.ui-history-card small {
		color: #93c5fd;
		font-size: 0.72rem;
		font-weight: 650;
	}

	.ui-history-card strong {
		color: #f4f4f5;
		font-size: 0.9rem;
	}

	.ui-history-card p {
		margin: 0;
		color: #a1a1aa;
		font-size: 0.82rem;
		line-height: 1.45;
	}

	.ui-file-chip {
		display: inline-flex;
		width: fit-content;
		margin-top: 0.2rem;
		padding: 0.28rem 0.55rem;
		border-radius: 0.5rem;
		background: rgba(255, 255, 255, 0.06);
		border: 1px solid rgba(255, 255, 255, 0.1);
		color: #d4d4d8;
		font-size: 0.72rem;
		font-weight: 600;
	}

	.workflow-intro {
		display: grid;
		gap: 0.85rem;
		justify-items: center;
		text-align: center;
	}

	.workflow-steps-grid {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		gap: 1.5rem;
	}

	.workflow-step-card {
		position: relative;
		display: grid;
		grid-template-columns: auto 1fr;
		grid-template-areas:
			'icon num'
			'icon title'
			'icon text';
		column-gap: 0.9rem;
		row-gap: 0.2rem;
		align-items: start;
		text-align: left;
		padding: 0.15rem 0;
	}

	.workflow-step-icon {
		grid-area: icon;
		display: grid;
		place-items: center;
		width: 2.5rem;
		height: 2.5rem;
		border-radius: 0.85rem;
		background: rgba(58, 111, 215, 0.18);
		border: 1px solid rgba(255, 255, 255, 0.1);
		color: #bfd6ff;
		font-size: 1.25rem;
		margin: 0.1rem 0 0;
	}

	.workflow-step-num {
		grid-area: num;
		color: #93c5fd;
		font-size: 0.72rem;
		font-weight: 750;
		letter-spacing: 0.08em;
	}

	.workflow-step-card .m3-title {
		grid-area: title;
	}

	.workflow-step-card .m3-body {
		grid-area: text;
	}

	.workflow-step-arrow {
		display: none;
	}

	.m3-title {
		font-size: 1.25rem;
		font-weight: 600;
		margin: 0;
		color: var(--m3-sys-light-on-surface);
	}

	@media (min-width: 900px) {
		.feature-showcase {
			gap: 5rem;
			justify-items: stretch;
		}

		.feature-stack {
			width: 100%;
			max-width: 44rem;
			gap: 1.35rem;
		}

		.feature-microblocks {
			gap: 1.35rem;
		}

		.feature-microblock-copy strong {
			font-size: 1.05rem;
		}

		.feature-microblock-copy p {
			font-size: 0.98rem;
			line-height: 1.6;
			max-width: 48ch;
		}

		.workflow-steps-grid {
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 1.25rem 2rem;
			align-items: start;
			margin-top: 0.5rem;
		}

		.workflow-step-card {
			grid-template-columns: 1fr;
			grid-template-areas:
				'icon'
				'num'
				'title'
				'text';
			justify-items: center;
			text-align: center;
			padding: 0.5rem 0.75rem;
			row-gap: 0.45rem;
		}

		.workflow-step-icon {
			width: 2.75rem;
			height: 2.75rem;
			font-size: 1.4rem;
			margin: 0 0 0.2rem;
		}

		.workflow-step-arrow {
			display: grid;
			place-items: center;
			position: absolute;
			top: 1.35rem;
			right: -1.15rem;
			color: rgba(147, 197, 253, 0.55);
			font-size: 1.25rem;
		}
	}

	.m3-cta-card {
		background: rgba(59, 130, 246, 0.1);
		border: 1px solid rgba(59, 130, 246, 0.3);
		border-radius: 2rem;
		padding: 3rem 1.5rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 1.5rem;
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		box-shadow: 0 0 80px rgba(59, 130, 246, 0.12), inset 0 1px 0 rgba(147, 197, 253, 0.1);
	}

	@media (min-width: 640px) {
		.m3-cta-card {
			padding: 4rem 2rem;
		}
	}

	.cta-content {
		max-width: 640px;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1.25rem;
	}

	.cta-btn {
		margin-top: 0.5rem;
	}

	.cta-reassure {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		margin: 0.15rem 0 0;
		color: #bbf7d0;
		font-size: 0.9rem;
		font-weight: 650;
	}

	.cta-reassure .material-symbols-rounded {
		font-size: 1.15rem;
		color: #86efac;
	}

	.m3-cta-card .m3-headline {
		color: #ffffff;
	}

	.m3-cta-card .m3-body-large {
		color: #a1a1aa;
	}

	.m3-footer {
		text-align: center;
		padding: 2rem 1rem 3rem;
		color: var(--m3-sys-light-on-surface-variant);
		font-size: 0.9rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.75rem;
	}

	.m3-footer-support {
		margin: 0;
		color: #a1a1aa;
		font-size: 0.9rem;
	}

	.m3-footer-nav {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 1rem;
	}

	.m3-footer-link {
		color: #bfd6ff;
		font-weight: 650;
	}
</style>
