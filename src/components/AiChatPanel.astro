<ai-chat-panel>
	<div class="ai-chat-shell" data-ai-chat-shell aria-hidden="true">
		<button type="button" class="ai-chat-backdrop" data-ai-chat-close aria-label="Cerrar asistente IA"></button>

		<aside class="ai-chat-drawer" role="dialog" aria-modal="true" aria-label="Asistente IA Hasel">
			<header class="ai-chat-header">
				<div class="ai-chat-title-wrap">
					<div class="ai-chat-mark">
						<span class="material-symbols-rounded">auto_awesome</span>
					</div>
					<div class="min-w-0">
						<h2 class="ai-chat-title">Asistente IA</h2>
						<p class="ai-chat-subtitle">Hasel</p>
					</div>
				</div>

				<div class="ai-chat-header-actions">
					<button
						type="button"
						class="ai-chat-history-toggle"
						data-ai-chat-history-toggle
						aria-controls="ai-chat-history-panel"
						aria-expanded="false"
					>
						<span class="material-symbols-rounded" aria-hidden="true">history</span>
						<span class="ai-chat-action-label">Historial</span>
					</button>

					<button type="button" class="ai-chat-new-btn" data-ai-chat-new>
						<span class="material-symbols-rounded" aria-hidden="true">add</span>
						Nuevo
					</button>

					<button type="button" class="ai-chat-icon-btn" data-ai-chat-close aria-label="Cerrar asistente IA">
						<span class="material-symbols-rounded">close</span>
					</button>
				</div>
			</header>

			<div class="ai-chat-layout">
				<button
					type="button"
					class="ai-chat-history-backdrop"
					data-ai-chat-history-close
					aria-label="Cerrar historial"
				></button>

				<section class="ai-chat-history" id="ai-chat-history-panel" aria-label="Historial de conversaciones">
					<div class="ai-chat-history-head">
						<h3>Historial</h3>
						<button type="button" class="ai-chat-icon-btn" data-ai-chat-history-close aria-label="Cerrar historial">
							<span class="material-symbols-rounded">close</span>
						</button>
					</div>

					<div class="ai-chat-history-state" data-ai-chat-sessions-state>Cargando historial...</div>
					<div class="ai-chat-sessions" data-ai-chat-sessions></div>
				</section>

				<section class="ai-chat-main">
					<div class="ai-chat-messages" data-ai-chat-messages>
						<div class="ai-chat-empty">
							<div class="ai-chat-empty-icon">
								<span class="material-symbols-rounded">auto_awesome</span>
							</div>
							<h3>Consulta tu agenda</h3>
							<p>Haz preguntas sobre clientes, turnos, disponibilidad o el estado del negocio.</p>
						</div>
					</div>

					<div class="ai-chat-tools" data-ai-chat-tools>
						<div class="ai-chat-tools-row">
							<button
								type="button"
								class="ai-chat-tools-btn"
								data-ai-chat-tools-toggle
								aria-controls="ai-chat-tools-menu"
								aria-expanded="false"
							>
								<span class="material-symbols-rounded" aria-hidden="true">construction</span>
								Herramientas
							</button>
						</div>

						<div
							class="ai-chat-quick-actions"
							id="ai-chat-tools-menu"
							data-ai-chat-quick-actions
							aria-label="Acciones rápidas del asistente"
						>
						</div>
					</div>

					<div class="ai-chat-error hidden" data-ai-chat-error></div>

					<form class="ai-chat-form" data-ai-chat-form>
						<textarea
							name="message"
							rows="1"
							maxlength="2000"
							placeholder="Escribe tu mensaje..."
							data-ai-chat-input
						></textarea>
						<button type="submit" class="ai-chat-send-btn" data-ai-chat-send aria-label="Enviar mensaje">
							<span class="material-symbols-rounded">send</span>
						</button>
					</form>
				</section>
			</div>
		</aside>
	</div>
</ai-chat-panel>

<script>
	import '../scripts/ai-chat-panel';
</script>

<style is:global>
	ai-chat-panel {
		display: contents;
	}

	body.ai-chat-open {
		overflow: hidden;
	}

	.ai-chat-shell {
		position: fixed;
		inset: 0;
		z-index: 80;
		pointer-events: none;
		overscroll-behavior: contain;
		touch-action: none;
	}

	.ai-chat-shell.is-open {
		pointer-events: auto;
	}

	.ai-chat-backdrop {
		position: absolute;
		inset: 0;
		border: 0;
		background: rgba(15, 23, 42, 0.22);
		opacity: 0;
		transition: opacity 180ms ease;
	}

	.ai-chat-shell.is-open .ai-chat-backdrop {
		opacity: 1;
	}

	.ai-chat-drawer {
		position: absolute;
		inset-block: 0;
		right: 0;
		display: grid;
		grid-template-rows: auto 1fr;
		width: min(46rem, 100vw);
		max-width: 100vw;
		border-left: 1px solid var(--shell-border);
		background: var(--surface-bright);
		color: var(--on-surface);
		box-shadow: -18px 0 48px rgba(15, 23, 42, 0.2);
		transform: translateX(100%);
		transition: transform 220ms cubic-bezier(0.16, 1, 0.3, 1);
	}

	.ai-chat-shell.is-open .ai-chat-drawer {
		transform: translateX(0);
	}

	.ai-chat-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		border-bottom: 1px solid var(--shell-border);
		background: color-mix(in srgb, var(--surface-bright) 90%, var(--primary-soft) 10%);
		padding: 1rem 1.15rem;
	}

	.ai-chat-title-wrap {
		display: flex;
		min-width: 0;
		align-items: center;
		gap: 0.75rem;
	}

	.ai-chat-mark,
	.ai-chat-empty-icon {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 1rem;
		background: color-mix(in srgb, var(--primary) 15%, transparent);
		color: var(--primary);
	}

	.ai-chat-mark {
		width: 2.75rem;
		height: 2.75rem;
		flex-shrink: 0;
	}

	.ai-chat-title {
		margin: 0;
		font-size: 1.05rem;
		font-weight: 850;
		line-height: 1.15;
	}

	.ai-chat-subtitle {
		margin: 0.12rem 0 0;
		font-size: 0.82rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: var(--on-surface-variant);
	}

	.ai-chat-header-actions {
		display: flex;
		flex-shrink: 0;
		align-items: center;
		gap: 0.5rem;
	}

	.ai-chat-icon-btn,
	.ai-chat-send-btn,
	.ai-chat-history-toggle,
	.ai-chat-tools-btn,
	.ai-chat-new-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border: 1px solid var(--shell-border);
		background: var(--surface-bright);
		color: var(--on-surface);
		transition: background-color 140ms ease, border-color 140ms ease, opacity 140ms ease;
	}

	.ai-chat-icon-btn {
		width: 2.5rem;
		height: 2.5rem;
		border-radius: 999px;
	}

	.ai-chat-history-toggle,
	.ai-chat-tools-btn,
	.ai-chat-new-btn {
		gap: 0.35rem;
		min-height: 2.5rem;
		border-radius: 999px;
		padding: 0.48rem 0.78rem;
		font-size: 0.8rem;
		font-weight: 850;
	}

	.ai-chat-history-toggle,
	.ai-chat-tools-btn {
		background: var(--surface-bright);
		color: var(--on-surface);
	}

	.ai-chat-new-btn {
		border-color: color-mix(in srgb, var(--primary) 50%, transparent);
		background: var(--primary);
		color: #f8fbff;
	}

	.ai-chat-history-toggle .material-symbols-rounded,
	.ai-chat-tools-btn .material-symbols-rounded,
	.ai-chat-new-btn .material-symbols-rounded {
		font-size: 1.05rem;
	}

	.ai-chat-icon-btn:hover,
	.ai-chat-send-btn:hover,
	.ai-chat-history-toggle:hover,
	.ai-chat-tools-btn:hover {
		background: var(--surface-container-high);
		border-color: color-mix(in srgb, var(--shell-border) 70%, var(--primary) 30%);
	}

	.ai-chat-new-btn:hover {
		border-color: var(--primary);
		background: color-mix(in srgb, var(--primary) 86%, #ffffff 14%);
	}

	.ai-chat-layout {
		position: relative;
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		min-height: 0;
		overflow: hidden;
	}

	.ai-chat-history-backdrop {
		position: absolute;
		inset: 0;
		z-index: 2;
		border: 0;
		background: rgba(15, 23, 42, 0.16);
		opacity: 0;
		pointer-events: none;
		transition: opacity 180ms ease;
	}

	.ai-chat-shell.is-history-open .ai-chat-history-backdrop {
		opacity: 1;
		pointer-events: auto;
	}

	.ai-chat-history {
		position: absolute;
		inset-block: 0;
		left: 0;
		z-index: 3;
		display: grid;
		grid-template-rows: auto auto minmax(0, 1fr);
		gap: 0.75rem;
		width: min(19rem, calc(100% - 1.25rem));
		min-height: 0;
		border-right: 1px solid var(--shell-border);
		background: var(--control-surface);
		padding: 1rem;
		box-shadow: 18px 0 36px rgba(15, 23, 42, 0.14);
		transform: translateX(calc(-100% - 1rem));
		transition: transform 220ms cubic-bezier(0.16, 1, 0.3, 1);
	}

	.ai-chat-shell.is-history-open .ai-chat-history {
		transform: translateX(0);
	}

	.ai-chat-history-head {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
	}

	.ai-chat-history-head h3 {
		margin: 0;
		font-size: 0.9rem;
		font-weight: 850;
		color: var(--on-surface);
	}

	.ai-chat-history-state {
		grid-row: 2;
		border-radius: 0.9rem;
		background: color-mix(in srgb, var(--surface-bright) 76%, transparent);
		padding: 0.75rem;
		font-size: 0.82rem;
		font-weight: 650;
		color: var(--on-surface-variant);
	}

	.ai-chat-history-state.hidden {
		display: none;
	}

	.ai-chat-sessions {
		grid-row: 3;
		display: grid;
		align-content: start;
		gap: 0.45rem;
		min-height: 0;
		overflow-y: auto;
		overscroll-behavior: contain;
		padding-right: 0.1rem;
		touch-action: pan-y;
	}

	.ai-chat-session-item {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		align-items: center;
		gap: 0.18rem;
		width: 100%;
		border: 1px solid transparent;
		border-radius: 0.9rem;
		background: transparent;
		padding: 0.18rem;
		transition: background-color 140ms ease, border-color 140ms ease;
	}

	.ai-chat-session-btn {
		display: grid;
		gap: 0.18rem;
		min-width: 0;
		border: 0;
		border-radius: 0.72rem;
		background: transparent;
		padding: 0.54rem;
		text-align: left;
		color: var(--on-surface);
	}

	.ai-chat-session-item:hover,
	.ai-chat-session-item.is-active {
		border-color: var(--shell-border);
		background: var(--surface-bright);
	}

	.ai-chat-session-delete-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2rem;
		height: 2rem;
		border: 0;
		border-radius: 999px;
		background: transparent;
		color: var(--on-surface-variant);
		opacity: 0;
		transition: background-color 140ms ease, color 140ms ease, opacity 140ms ease;
	}

	.ai-chat-session-item:hover .ai-chat-session-delete-btn,
	.ai-chat-session-item:focus-within .ai-chat-session-delete-btn,
	.ai-chat-session-item.is-active .ai-chat-session-delete-btn {
		opacity: 1;
	}

	.ai-chat-session-delete-btn:hover {
		background: rgba(239, 68, 68, 0.12);
		color: #ef4444;
	}

	.ai-chat-session-delete-btn:disabled {
		cursor: not-allowed;
		opacity: 0.45;
	}

	.ai-chat-session-delete-btn .material-symbols-rounded {
		font-size: 1.05rem;
	}

	.ai-chat-session-title {
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
		font-size: 0.84rem;
		font-weight: 800;
	}

	.ai-chat-session-date {
		font-size: 0.72rem;
		font-weight: 650;
		color: var(--on-surface-variant);
	}

	.ai-chat-main {
		display: grid;
		grid-template-rows: 1fr auto auto auto;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		background: var(--surface-bright);
	}

	.ai-chat-messages {
		display: flex;
		flex-direction: column;
		gap: 0.8rem;
		min-height: 0;
		overflow-y: auto;
		overscroll-behavior: contain;
		padding: 1.1rem;
		touch-action: pan-y;
	}

	.ai-chat-empty {
		margin: auto;
		display: flex;
		max-width: 22rem;
		flex-direction: column;
		align-items: center;
		text-align: center;
		color: var(--on-surface-variant);
	}

	.ai-chat-empty-icon {
		width: 3.8rem;
		height: 3.8rem;
		margin-bottom: 0.9rem;
		border-radius: 999px;
	}

	.ai-chat-empty h3 {
		margin: 0;
		font-size: 1rem;
		font-weight: 850;
		color: var(--on-surface);
	}

	.ai-chat-empty p {
		margin: 0.45rem 0 0;
		font-size: 0.9rem;
		font-weight: 600;
		line-height: 1.45;
	}

	.ai-chat-message {
		display: grid;
		max-width: min(88%, 34rem);
		gap: 0.25rem;
	}

	.ai-chat-message--user {
		align-self: flex-end;
	}

	.ai-chat-message--assistant {
		align-self: flex-start;
	}

	.ai-chat-bubble {
		border: 1px solid var(--shell-border);
		border-radius: 1.05rem;
		padding: 0.75rem 0.88rem;
		overflow-wrap: anywhere;
		font-size: 0.92rem;
		font-weight: 600;
		line-height: 1.45;
	}

	.ai-chat-message--user .ai-chat-bubble {
		border-color: color-mix(in srgb, var(--primary) 50%, transparent);
		background: var(--primary);
		color: #f8fbff;
		border-bottom-right-radius: 0.35rem;
		white-space: pre-wrap;
	}

	.ai-chat-message--assistant .ai-chat-bubble {
		background: var(--control-surface);
		color: var(--on-surface);
		border-bottom-left-radius: 0.35rem;
	}

	.ai-chat-message--assistant .ai-chat-bubble > :first-child {
		margin-top: 0;
	}

	.ai-chat-message--assistant .ai-chat-bubble > :last-child {
		margin-bottom: 0;
	}

	.ai-chat-message--assistant .ai-chat-bubble p,
	.ai-chat-message--assistant .ai-chat-bubble ul,
	.ai-chat-message--assistant .ai-chat-bubble ol,
	.ai-chat-message--assistant .ai-chat-bubble blockquote,
	.ai-chat-message--assistant .ai-chat-bubble pre,
	.ai-chat-message--assistant .ai-chat-bubble table {
		margin: 0.55rem 0;
	}

	.ai-chat-message--assistant .ai-chat-bubble ul,
	.ai-chat-message--assistant .ai-chat-bubble ol {
		padding-left: 1.2rem;
	}

	.ai-chat-message--assistant .ai-chat-bubble li + li {
		margin-top: 0.18rem;
	}

	.ai-chat-message--assistant .ai-chat-bubble h1,
	.ai-chat-message--assistant .ai-chat-bubble h2,
	.ai-chat-message--assistant .ai-chat-bubble h3,
	.ai-chat-message--assistant .ai-chat-bubble h4,
	.ai-chat-message--assistant .ai-chat-bubble h5,
	.ai-chat-message--assistant .ai-chat-bubble h6 {
		margin: 0.65rem 0 0.35rem;
		font-size: 0.98rem;
		font-weight: 850;
		line-height: 1.25;
	}

	.ai-chat-message--assistant .ai-chat-bubble strong {
		font-weight: 850;
	}

	.ai-chat-message--assistant .ai-chat-bubble blockquote {
		border-left: 3px solid color-mix(in srgb, var(--primary) 45%, transparent);
		padding-left: 0.75rem;
		color: var(--on-surface-variant);
	}

	.ai-chat-message--assistant .ai-chat-bubble table {
		display: block;
		max-width: 100%;
		overflow-x: auto;
		border-collapse: collapse;
	}

	.ai-chat-message--assistant .ai-chat-bubble th,
	.ai-chat-message--assistant .ai-chat-bubble td {
		border: 1px solid var(--shell-border);
		padding: 0.35rem 0.5rem;
		text-align: left;
	}

	.ai-chat-message--assistant .ai-chat-bubble a {
		color: var(--primary);
		font-weight: 800;
		text-decoration: underline;
		text-underline-offset: 0.16em;
	}

	.ai-chat-message--assistant .ai-chat-bubble code {
		border-radius: 0.35rem;
		background: color-mix(in srgb, var(--on-surface) 9%, transparent);
		padding: 0.08rem 0.24rem;
		font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
		font-size: 0.86em;
	}

	.ai-chat-message--assistant .ai-chat-bubble pre {
		overflow-x: auto;
		border-radius: 0.65rem;
		background: color-mix(in srgb, var(--on-surface) 9%, transparent);
		padding: 0.7rem;
	}

	.ai-chat-message--assistant .ai-chat-bubble pre code {
		background: transparent;
		padding: 0;
	}

	.ai-chat-tools {
		display: grid;
		gap: 0.55rem;
		border-top: 1px solid color-mix(in srgb, var(--shell-border) 72%, transparent);
		background: color-mix(in srgb, var(--surface-bright) 94%, var(--control-surface) 6%);
		padding: 0.65rem 1.1rem 0.35rem;
	}

	.ai-chat-tools-row {
		display: flex;
		justify-content: flex-end;
	}

	.ai-chat-quick-actions {
		display: none;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.45rem;
		max-height: min(15rem, 36vh);
		overflow-y: auto;
		overscroll-behavior: contain;
		border: 1px solid var(--shell-border);
		border-radius: 1rem;
		background: var(--surface-bright);
		padding: 0.65rem;
		box-shadow: 0 14px 34px rgba(15, 23, 42, 0.12);
		touch-action: pan-y;
	}

	.ai-chat-shell.is-tools-open .ai-chat-quick-actions {
		display: grid;
	}

	.ai-chat-quick-action-btn {
		display: inline-flex;
		align-items: center;
		justify-content: flex-start;
		gap: 0.38rem;
		width: 100%;
		min-height: 2.55rem;
		border: 1px solid color-mix(in srgb, var(--shell-border) 76%, var(--primary) 24%);
		border-radius: 0.82rem;
		background: color-mix(in srgb, var(--primary-soft, var(--primary)) 18%, var(--surface-bright) 82%);
		padding: 0.58rem 0.68rem;
		font-size: 0.8rem;
		font-weight: 800;
		line-height: 1.15;
		text-align: left;
		color: var(--primary);
		transition: background-color 140ms ease, border-color 140ms ease, opacity 140ms ease;
	}

	.ai-chat-quick-action-btn:hover {
		border-color: color-mix(in srgb, var(--shell-border) 54%, var(--primary) 46%);
		background: color-mix(in srgb, var(--primary-soft, var(--primary)) 28%, var(--surface-bright) 72%);
	}

	.ai-chat-quick-action-btn:disabled {
		cursor: not-allowed;
		opacity: 0.52;
	}

	.ai-chat-quick-action-btn .material-symbols-rounded {
		font-size: 1rem;
	}

	.ai-chat-error {
		margin: 0 1.1rem 0.75rem;
		border-radius: 0.9rem;
		border: 1px solid rgba(239, 68, 68, 0.24);
		background: rgba(239, 68, 68, 0.1);
		padding: 0.75rem 0.9rem;
		font-size: 0.86rem;
		font-weight: 750;
		color: #ef4444;
	}

	.ai-chat-form {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		gap: 0.65rem;
		border-top: 1px solid var(--shell-border);
		background: color-mix(in srgb, var(--surface-bright) 92%, var(--control-surface) 8%);
		padding: 0.9rem 1.1rem;
	}

	.ai-chat-form textarea {
		min-height: 2.75rem;
		max-height: 8rem;
		resize: none;
		border: 1px solid var(--shell-border);
		border-radius: 1rem;
		background: var(--surface-bright);
		padding: 0.78rem 0.9rem;
		font: inherit;
		font-size: 0.92rem;
		font-weight: 650;
		color: var(--on-surface);
		outline: none;
	}

	.ai-chat-form textarea:focus {
		border-color: var(--primary);
		box-shadow: 0 0 0 4px color-mix(in srgb, var(--primary) 14%, transparent);
	}

	.ai-chat-send-btn {
		width: 2.75rem;
		height: 2.75rem;
		border-radius: 1rem;
		background: var(--primary);
		color: #f8fbff;
	}

	.ai-chat-send-btn:disabled {
		cursor: not-allowed;
		opacity: 0.55;
	}

	html[data-theme='dark'] .ai-chat-backdrop {
		background: rgba(0, 0, 0, 0.44);
	}

	html[data-theme='dark'] .ai-chat-drawer {
		box-shadow: -18px 0 48px rgba(0, 0, 0, 0.42);
	}

	html[data-theme='dark'] .ai-chat-header {
		background: color-mix(in srgb, var(--surface-container, #1e1f22) 90%, var(--primary) 10%);
	}

	html[data-theme='dark'] .ai-chat-history {
		background: var(--surface-container, #1e1f22);
		box-shadow: 18px 0 36px rgba(0, 0, 0, 0.34);
	}

	html[data-theme='dark'] .ai-chat-history-backdrop {
		background: rgba(0, 0, 0, 0.28);
	}

	html[data-theme='dark'] .ai-chat-tools,
	html[data-theme='dark'] .ai-chat-quick-actions {
		background: var(--surface-container, #1e1f22);
	}

	html[data-theme='dark'] .ai-chat-form {
		background: var(--surface-container, #1e1f22);
	}

	@media (max-width: 760px) {
		.ai-chat-drawer {
			width: 100vw;
		}

		.ai-chat-header {
			gap: 0.65rem;
			padding: 0.85rem 0.95rem;
		}

		.ai-chat-mark {
			width: 2.5rem;
			height: 2.5rem;
		}

		.ai-chat-header-actions {
			gap: 0.4rem;
		}

		.ai-chat-history-toggle,
		.ai-chat-new-btn {
			min-height: 2.35rem;
			padding: 0.45rem 0.65rem;
		}

		.ai-chat-icon-btn {
			width: 2.35rem;
			height: 2.35rem;
		}

		.ai-chat-history {
			width: min(20rem, calc(100% - 0.75rem));
			padding: 0.9rem;
		}

		.ai-chat-messages {
			padding: 1rem;
		}

		.ai-chat-session-delete-btn {
			opacity: 1;
		}

		.ai-chat-tools {
			padding: 0.6rem 1rem 0.3rem;
		}

		.ai-chat-quick-actions {
			max-height: min(13rem, 34vh);
		}

		.ai-chat-quick-action-btn {
			font-size: 0.76rem;
		}

		.ai-chat-form {
			padding: 0.75rem 1rem;
		}
	}

	@media (max-width: 430px) {
		.ai-chat-title-wrap {
			gap: 0.55rem;
		}

		.ai-chat-title {
			font-size: 0.98rem;
		}

		.ai-chat-subtitle {
			font-size: 0.74rem;
		}

		.ai-chat-history-toggle {
			width: 2.35rem;
			padding-inline: 0;
		}

		.ai-chat-history-toggle .ai-chat-action-label {
			display: none;
		}

		.ai-chat-quick-actions {
			grid-template-columns: 1fr;
		}
	}
</style>
